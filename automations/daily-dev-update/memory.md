# 每日開發工具更新（Claude Code 版）記錄

排程：每天 09:00，launchd `com.po-chi.claude-daily-dev-update`。
與 Codex 的 `每日開發工具更新`（已改到 14:00）並存，兩份互相對照。

每次執行由 Claude 自己在下方**附加**一節，不要覆蓋既有內容。

---

## 2026-07-27 建立

- 建立 `~/.claude/automations/daily-dev-update/`：`PROMPT.md`（工作定義）、`run.sh`（launchd 進入點）、`logs/`（每日輸出）。
- 這份的存在理由：Codex 端同名 automation 曾連續被「唯讀檔案系統 + 封鎖 DNS」的 managed sandbox 擋住。後續 rollout 證據已排除 project trust 與 Mac 檔案權限問題；限制是由 Desktop 背景 scheduler 在首個 turn context 前注入。
- 因此更新執行走 user-level launchd + Codex CLI，不經過 Codex Desktop Scheduled permission resolver；Desktop Scheduled 僅唯讀回報結果。

## 2026-08-03T15:36:59+0800 daily development-tool update

- Completed the unattended user-level update from `$HOME` without entering a project, changing project dependencies, starting MCP servers, or touching any repository graph, hook, `.mcp.json`, lockfile, or uncommitted work.
- Claude Code remained current at 2.1.220. `claude doctor` confirms a healthy native install, bundled search, and enabled latest-channel auto-updates. Refreshed all five configured marketplaces with official commands. The mattpocock marketplace moved `ed37663 -> 2ab9580` with only `README.md` changed; no upstream skill was added, so the self-held manifest and symlink were preserved unchanged.
- Updated the Claude `code-review` plugin `75ee6cb1523b -> 627c680174dd`; all other installed plugins retained their versions and enabled/disabled state. Claude Code must restart to load this plugin update.
- Codex remained current at 0.146.0 through its official updater, which requested a restart. `codex plugin marketplace upgrade` found no configured Git marketplace. Bundled, primary-runtime, and App-managed marketplaces were left unchanged.
- Updated Homebrew metadata/runtime `6.0.13 -> 6.0.14`, then CLI formulae: gh `2.96.0 -> 2.97.0`, mise `2026.7.14 -> 2026.8.0`, Mole `1.48.1 -> 1.49.1`, mpg123 `1.33.6 -> 1.33.7`, poppler `26.07.0 -> 26.08.0`, Redis `8.8.1 -> 8.10.0`, usage `4.0.0 -> 4.1.0`, and Supabase CLI `2.110.0 -> 2.111.0`. Poppler's required dependency transaction also updated glib `2.88.2 -> 2.88.3` and NSS `3.125 -> 3.126`; neither library was upgraded directly.
- Updated global Node CLIs: `@earendil-works/pi-coding-agent 0.82.1 -> 0.83.0`, Shopify CLI `4.5.2 -> 4.6.0`, pnpm `11.17.0 -> 11.18.0`, and Vercel CLI `58.4.0 -> 58.4.4`. npm stayed at 11.16.0 because 12.0.2 is a major migration. Bun stayed current at 1.3.14; there are no Bun globals.
- Updated uv `0.11.32 -> 0.12.1`. `code-review-graph` stayed at 2.3.7 and `voice-mode` at 8.12.0 while their isolated dependencies refreshed; pyright stayed at 1.1.411. Only `uv tool upgrade code-review-graph` and `code-review-graph --version` were used for CRG; no install/build/update/postprocess/watch/daemon/embed/server or graph migration command ran. Graphify/graphifyy remained untouched and absent from the update path.
- Updated the fixed Context7 MCP package reference `3.2.4 -> 3.2.5` after official npm registry resolution. GCP MCP 0.5.3 and Serena 1.6.1 are current/resolvable. Codex reports nine locally consistent MCP configurations; no server was started, no OAuth flow was attempted, and no secret or project-scope configuration changed.
- Rust stable/rustc/cargo stayed at 1.97.1, rustup at 1.29.0, Corepack at 0.35.0, and installed mise runtimes were not changed. No Cargo-installed tools were present.
- Skipped GUI casks, macOS/Xcode/Simulator updates, language-runtime updates, and remaining non-CLI/library formulae: certifi, harfbuzz, OpenJDK, OpenJDK 21, and pgvector. Homebrew's untrusted taps remained ignored; trust protections were not bypassed.
- Final version checks passed. Codex doctor confirms install/config/auth/network/MCP/database health; its overall failure is the expected unattended `TERM=dumb` check, plus the existing rollout/state inventory warning (572 stale rows). No package update remains failed. Initial pnpm attempts lacked its configured global bin path, then succeeded using the existing user-level PNPM location without running `pnpm setup`.
- Next run: do not retry npm 12, language runtimes/libraries, untrusted taps, Graphify, CRG installer/schema work, App-managed marketplaces, or local/custom skills unless the governing rules change. Restart Claude Code and Codex is still pending.

## 2026-08-05T09:07:36+0800 daily development-tool update

- Completed the unattended user-level update from `$HOME`. No project dependency command ran, no repository was entered for maintenance, and no project `.mcp.json`, graph database, hook, lockfile, repository file, or uncommitted work was changed.
- Updated Claude Code through `claude update` from 2.1.221 to 2.1.222. `claude doctor` confirms a healthy native install, bundled search, enabled latest-channel auto-updates, and no installation issues.
- Refreshed all five configured Claude marketplaces with official commands: claude-plugins-official, anthropic-agent-skills, openai-codex, karpathy-skills, and mattpocock. The four Git-backed marketplace heads inspected before and after were unchanged; all six installed marketplace plugins were explicitly checked with `claude plugin update` and remained current, including the disabled state of ralph-loop. `codex plugin marketplace upgrade` correctly reported no configured Git marketplaces; bundled, primary-runtime, and App-managed marketplaces were left unchanged.
- Preserved the self-held `~/.claude/skills/mattpocock-skills` manifest byte-for-byte (SHA-256 `924a2d86a3221f4df7f2931b7b610b3fc35e46049948aa74c72238bf9b1b9984`) and retained its marketplace symlink. The mattpocock Git head did not change, so no upstream skill was added. All local/custom Claude and shared Codex-source skills were left untouched.
- Codex remained current at 0.146.0 through `codex update`; the official updater requested a restart. Codex Doctor reports a consistent standalone install, healthy config/auth/network/databases, nine locally consistent MCP configurations, and zero failures. The pre-existing state inventory warning remains (417 stale rollout rows); no cleanup was attempted.
- Updated Homebrew metadata/runtime 6.0.14 -> 6.0.15, then CLI formulae: herdr 0.7.5 -> 0.8.0, just 1.57.0 -> 1.58.0, mise 2026.8.0 -> 2026.8.1, and Mole 1.49.1 -> 1.49.2. Homebrew's untrusted-tap protection was not bypassed.
- Updated the pnpm global Vercel CLI 58.4.4 -> 58.5.1. npm 11.16.0, pnpm 11.18.0, Corepack 0.35.0, Bun 1.3.14, pi-coding-agent 0.83.0, and Shopify CLI 4.6.0 stayed current. npm 12.0.2 was skipped as a major migration; Bun has no initialized global package set.
- uv stayed current at 0.12.1. `code-review-graph` stayed at 2.3.7 while isolated dependencies refreshed; pyright stayed at 1.1.411, and voice-mode stayed at 8.12.0 while isolated dependencies refreshed. CRG was touched only with `uv tool upgrade code-review-graph` and verified with `code-review-graph --version`; no install/build/update/postprocess/watch/daemon/embed/server/schema/graph command ran. Graphify/graphifyy remained untouched and absent from the update path.
- Fixed MCP package references are still current by official package resolution: Context7 3.2.5, GCP MCP 0.5.3, and Serena 1.6.1. Floating Context7 and Playwright references resolve. No MCP server was started, no OAuth flow was attempted, no secret was printed or rewritten, and no fixed MCP version or config required a change.
- gh 2.97.0, Rust stable/rustc/cargo 1.97.1, rustup 1.29.0, and installed mise runtimes stayed current; no Cargo-installed tools were present. gh authentication remains healthy.
- Skipped GUI casks block-buzz 0.5.2 -> 0.5.3, ngrok 3.39.8 -> 3.39.10, and Orca 1.4.162 -> 1.4.165; macOS/Xcode/Simulator updates; usage 4.1.0 -> 5.0.0 as a major CLI migration; language runtimes OpenJDK 26.0.1 -> 26.0.2 and OpenJDK 21.0.11 -> 21.0.12; and non-CLI/library formulae certifi, harfbuzz, pgvector, and SDL3.
- Final version and health checks passed with no failed update item. Existing Claude Code and Codex sessions should be restarted to load the refreshed native binaries/process state. Next run should not retry npm 12, usage 5, GUI casks, language runtimes/libraries, untrusted taps, Graphify, CRG installer/schema work, App-managed marketplaces, or local/custom skills unless the governing rules change.

## 2026-08-06T10:17:14+0800 daily development-tool update

- Completed the unattended user-level update from `$HOME`. No project dependency operation ran, no repository was entered for maintenance, and no project `.mcp.json`, graph database, hook, lockfile, repository file, secret, or uncommitted work was changed.
- Claude Code stayed current at 2.1.223. `claude doctor` confirms a healthy native install, bundled search, enabled latest-channel auto-updates, and no installation issues. All five configured Claude marketplaces refreshed successfully; installed plugin versions and enabled/disabled states remained unchanged.
- The mattpocock marketplace advanced `2ab958093e83 -> 8b36d4fb2635`. Upstream promoted/added `wizard`, `to-questionnaire`, `wait-what`, and `writing-for-agents`, and removed `writing-great-skills`. The self-held manifest was preserved byte-for-byte at SHA-256 `924a2d86a3221f4df7f2931b7b610b3fc35e46049948aa74c72238bf9b1b9984`; as a result, Claude now reports the expected manifest error for the removed `writing-great-skills` path. This requires manual manifest review and was intentionally not repaired automatically. All other local/custom/shared skills were untouched.
- Updated Codex standalone `0.146.0 -> 0.146.1`. `codex plugin marketplace upgrade` reported no configured Git marketplaces; bundled, primary-runtime, curated, and App-managed marketplaces remained runtime/App managed. Codex Doctor confirms installation, config, auth, network, databases, update status, and nine MCP configurations are healthy; overall status remains failed only because unattended `TERM=dumb`, with the existing warning for 417 stale rollout rows.
- Refreshed Homebrew metadata `31b858ec89 -> a084a7df0e`. Updated mise `2026.8.1 -> 2026.8.2`; Homebrew unexpectedly completed its required transitive major dependency update usage `4.1.0 -> 5.0.0` before the attempted termination took effect. No unofficial rollback or manual relinking was attempted. Future runs must inspect the complete Homebrew dependency transaction before starting a formula upgrade and must not auto-downgrade usage.
- Updated pnpm `11.18.0 -> 11.20.0` through Corepack and pnpm-global Vercel CLI `58.5.1 -> 58.7.1`. npm 11.16.0, Corepack 0.35.0, Bun 1.3.14, pi-coding-agent 0.83.0, and Shopify CLI 4.6.0 stayed current. npm 12.0.2 remained skipped as a major migration; Bun still has no initialized global package set.
- Updated uv `0.12.1 -> 0.12.2`. `code-review-graph` stayed at 2.3.7 while only isolated dependencies refreshed; pyright stayed at 1.1.411 and voice-mode at 8.12.0. CRG was touched only with `uv tool upgrade code-review-graph` and `code-review-graph --version`; no install/build/update/postprocess/watch/daemon/embed/server/schema/graph command ran. Graphify/graphifyy remained untouched.
- Fixed MCP references remain current by official registry metadata: Context7 3.2.5, GCP MCP 0.5.3, and Serena 1.6.1; floating Context7 and Playwright resolve. No MCP configuration, fixed pin, OAuth flow, secret, or project-scope setting changed. Claude reports Notion and Slack need authentication; Codex reports `linkedin-profile` not logged in. Linear, Supabase, and Cloudflare did not report an authorization failure.
- gh 2.97.0 and Mole 1.49.2 stayed current; Mole's official updater completed successfully. Rust stable/rustc/cargo 1.97.1 and rustup 1.29.0 stayed current, with no Cargo-installed tools. Installed mise runtimes were not changed.
- Skipped GUI casks block-buzz `0.5.2 -> 0.5.5`, ngrok `3.39.8 -> 3.39.10`, and Orca `1.4.168 -> 1.4.173`; macOS/Xcode/Simulator updates; language runtimes OpenJDK `26.0.1 -> 26.0.2` and OpenJDK 21 `21.0.11 -> 21.0.12`; and non-CLI/library formulae certifi, harfbuzz, pango, pgvector, and SDL3. Homebrew's untrusted-tap protection was not bypassed.
- Final CLI/version checks passed. Restart Codex to load 0.146.1; restart/reload Claude Code after the mattpocock manifest is manually reconciled. Next run must not retry OAuth logins, rewrite the mattpocock manifest, update local/custom skills, downgrade usage, upgrade npm 12, touch GUI casks/language runtimes/libraries/untrusted taps, install Graphify, run CRG installer/schema/graph commands, or update App-managed marketplaces unless the governing rules change.

## 2026-08-07T09:09:33+0800 daily development-tool update

- Completed the unattended user-level update from `$HOME`. No repository was entered for maintenance; no project dependency, `.mcp.json`, graph database, hook, lockfile, repository file, secret, or uncommitted work was changed, and no MCP server was started.
- Claude Code stayed current at 2.1.223. `claude doctor` confirms a healthy native install, bundled search, enabled latest-channel auto-updates, and no installation issues. All five configured Claude marketplaces refreshed successfully, and all six installed marketplace plugins remained current with their enabled/disabled states preserved.
- The mattpocock marketplace advanced `8b36d4fb2635 -> 84fdeffd12f2`; this release modified existing skills/docs and added no new skill. The self-held manifest remained byte-for-byte unchanged at SHA-256 `924a2d86a3221f4df7f2931b7b610b3fc35e46049948aa74c72238bf9b1b9984`. Its known stale `writing-great-skills` entry still causes the expected load error and remains an intentional manual-review item. All local/custom/shared skills were untouched.
- Codex stayed current at 0.146.1 through its official updater, which requested a restart. `codex plugin marketplace upgrade` found no configured Git marketplace; bundled, primary-runtime, curated, and App-managed marketplaces were left unchanged. Codex Doctor reports healthy install/config/auth/network/databases/update status and nine locally consistent MCP configurations; overall status is warning only because of the existing 417 stale rollout rows.
- Refreshed Homebrew metadata `a084a7df0e -> 73720671af`. No installed CLI formula was outdated after refresh. Skipped GUI casks block-buzz `0.5.2 -> 0.5.5`, ngrok `3.39.8 -> 3.39.10`, and Orca `1.4.168 -> 1.4.175`; language runtimes OpenJDK `26.0.1 -> 26.0.2`, OpenJDK 21 `21.0.11 -> 21.0.12`, and Python 3.13 `3.13.14_1 -> 3.13.15`; and non-CLI/library formulae certifi, fontconfig, harfbuzz, p11-kit, pango, pgvector, and SDL3. No untrusted-tap protection was bypassed.
- Updated global npm CLI `@earendil-works/pi-coding-agent 0.83.0 -> 0.84.0`; `pi --version` passed. npm emitted its existing install-script policy warning for two transitive packages, but the installation exited successfully. npm 11.16.0, Shopify CLI 4.6.0, Corepack 0.35.0, pnpm 11.20.0, Vercel CLI 58.7.1, Bun 1.3.14, gh 2.97.0, Mole 1.49.2, mise 2026.8.2, rustup 1.29.0, and Rust/cargo 1.97.1 stayed current. npm 12.0.2 remained skipped as a major migration; Bun has no initialized global package set; installed mise runtimes were not changed.
- uv stayed current at 0.12.2. `code-review-graph` stayed at 2.3.7, pyright at 1.1.411, and voice-mode at 8.12.0. CRG was touched only with `uv tool upgrade code-review-graph` and `code-review-graph --version`; no install/build/update/postprocess/watch/daemon/embed/server/schema/graph command ran, and Graphify/graphifyy remained untouched.
- MCP package resolution remains healthy without starting servers: Context7 3.2.5, GCP MCP 0.5.3, Serena 1.6.1, and floating Playwright MCP 0.0.79 resolve from their official registries. No pin, config, OAuth flow, secret, or project-scope setting changed. No new Linear/Supabase/Cloudflare authorization failure was observed; the existing Codex `linkedin-profile` not-logged-in status was left unchanged.
- Final version and health checks passed; there were no failed update items. Restart Codex to load the refreshed standalone process state. The mattpocock manifest still needs human reconciliation. Next run must not retry OAuth logins, rewrite that manifest, update local/custom skills, upgrade npm 12, touch GUI casks/language runtimes/libraries/untrusted taps, install Graphify, run CRG installer/schema/graph commands, or update App-managed marketplaces unless the governing rules change.

## 2026-08-10T09:45:33+0800 daily development-tool update

- Completed the unattended user-level update from `$HOME` without entering any product repository or the Orca system-automations workspace. No project dependency, `.mcp.json`, graph database, hook, lockfile, repository worktree, secret, OAuth flow, or scheduler configuration was changed, and no MCP server was started.
- Claude Code stayed current at 2.1.226 through `claude update`. `claude doctor` confirms a healthy native install, bundled search, latest-channel auto-updates, and no installation issue; it retains a historical failed automatic attempt from 2026-08-09. All five configured marketplaces refreshed successfully. Their Git heads and all six user-scope plugin versions remained unchanged; enabled/disabled state was preserved.
- The mattpocock marketplace stayed at `84fdeffd12f2` with upstream manifest 1.2.3. The self-held manifest already matched `desired = upstream - handoff`; all 24 paths exist, `claude plugin validate` passed, Claude loaded the skill directory, and every Matt-managed Codex copy matched upstream. No local/custom skill was changed. Upstream HEAD did not change, so there was no new completion/external-write/commit/publish semantic delta to apply.
- Codex stayed current at 0.147.0 through its official updater and requires restart. `codex plugin marketplace upgrade` found all configured Git marketplaces current; bundled, primary-runtime, curated, and App-managed marketplaces were left unchanged. Codex Doctor reports 0 failures, healthy install/config/auth/network/databases and nine MCP configs; existing notes remain for 417 stale thread rows and an HTTP 403 latest-version probe.
- Refreshed Homebrew metadata `73720671af -> 4e6619a370`, then updated CLI formulae: mise `2026.8.2 -> 2026.8.3`, Mole `1.49.2 -> 1.50.0`, Supabase CLI `2.111.0 -> 2.113.0`, and usage `5.0.0 -> 5.1.0`. Dry runs confirmed the transaction before upgrading, and untrusted-tap protection was not bypassed.
- Updated global Node CLIs: `@earendil-works/pi-coding-agent 0.84.0 -> 0.84.1`, Shopify CLI `4.6.0 -> 4.6.1`, and Vercel CLI `58.7.1 -> 58.9.0`. npm stayed at 11.16.0 because 12.0.2 is a major migration; pnpm stayed at 11.20.0. npm reported existing allow-scripts warnings for transitive packages but both updates exited successfully. Bun's official updater failed with HTTP 403, so Bun stayed at 1.3.14 and no curl fallback was used.
- Updated uv `0.12.2 -> 0.12.3`. `code-review-graph` stayed at 2.3.7 while isolated dependencies refreshed; it was touched only with `uv tool upgrade code-review-graph` and verified with `code-review-graph --version`. Voice Mode stayed at 8.12.0 while isolated dependencies refreshed; pyright stayed at 1.1.411. No CRG install/build/update/postprocess/watch/daemon/embed/server/schema/graph command ran, and Graphify/graphifyy remained untouched.
- MCP package resolution succeeded without starting servers: Claude's floating Context7 4.0.0 and Playwright 0.0.79 resolve; GCP MCP 0.5.3 remains current. Codex fixed pins Context7 3.2.5 and disabled Serena 1.6.1 were not changed because registry latests 4.0.0 and 1.7.0 lack reviewed compatibility/migration evidence. No MCP config or authorization state changed.
- Rust stable/rustc/cargo 1.97.1, rustup 1.29.0, gh 2.97.0, Corepack 0.35.0, and installed mise runtimes stayed current/unchanged; no Cargo-installed tools or Bun globals exist. Skipped GUI casks block-buzz and ngrok, macOS/Xcode/Simulator, language runtimes OpenJDK/OpenJDK 21/Python 3.13, and non-CLI libraries certifi, fontconfig, harfbuzz, nspr, p11-kit, pango, pgvector, and SDL3.
- Mechanical semantic audit found two manual items: the Claude ship adapter is missing and the Codex ship adapter is not a pointer to `~/.agents/skills/ship/SKILL.md`; `~/.claude/skills/fugu-advisor` and `~/.claude/skills/fugu-worker` still record compatibility verification against Codex 0.146.0 while the installed version is 0.147.0. Both handoff skills correctly keep the primary checkout's `MEMORY.md` as the only persistence target. No policy/adapter/custom-skill rewrite was attempted.
- Next run: retry only Bun's official updater; do not use its curl fallback automatically. Do not auto-upgrade npm 12, fixed MCP major/minor pins, GUI casks, language runtimes/libraries, untrusted taps, Graphify, CRG installer/schema/graph work, App-managed marketplaces, local/custom skills, ship adapters, or Fugu compatibility text without governing evidence or manual review.

## 2026-08-11T09:38:28+0800 daily development-tool update

- Completed the unattended user-level update from `$HOME` without entering any product repository or the Orca system-automations workspace. No project dependency, `.mcp.json`, graph database, hook, lockfile, repository worktree, secret, OAuth flow, or scheduler configuration was changed, and no MCP server was started.
- Claude Code stayed current at 2.1.227 through `claude update`. `claude doctor` confirms a healthy native install, bundled search, latest-channel auto-updates, and no installation issue. Refreshed all seven configured marketplaces with official commands: claude-plugins-official, anthropic-agent-skills, openai-codex, karpathy-skills, mattpocock, marketingskills, and addy-web-quality-skills. All six user-scope installed plugins stayed current with enabled/disabled state preserved; project-scope plugin installations were not mutated.
- The mattpocock marketplace stayed at `84fdeffd12f2` with upstream manifest 1.2.3. Before refresh, all 24 Matt-managed Codex copies matched the old upstream. After refresh, `uv run python ~/.claude/scripts/reconcile_matt_manifest.py --write --runtime` confirmed the self-held manifest and Claude runtime inventory are current at 24 skills; `claude plugin validate` passed and every managed Codex copy still matches upstream. No local/custom skill changed and no upstream semantic delta existed to review.
- Codex stayed current at 0.147.0 through its official updater and requested restart. `codex plugin marketplace upgrade` selected the configured Shopify Git marketplace and found no root to upgrade; bundled, primary-runtime, curated, and App-managed marketplaces remained runtime/App managed. Codex Doctor reports healthy installation, config, auth, provider network, databases, update state, and nine locally consistent MCP configurations; overall status is warning only for the existing 417 stale rollout rows.
- Homebrew metadata/runtime updated `6.0.15-167-g4e6619a -> 6.0.16-2-g007333f` (`4e6619a370 -> 007333f25e`). No installed CLI formula remained outdated. Untrusted-tap protection was not bypassed.
- Updated shared Node CLIs: pnpm `11.20.0 -> 11.21.0` through the active NVM Corepack 0.35.0, and pnpm-global Vercel CLI `58.9.0 -> 58.9.1`. The first pnpm attempt resolved the stale `/usr/local/bin/corepack` 0.28.0 and failed signature verification; retrying with the active NVM Corepack succeeded, so there is no remaining package failure. npm 11.16.0, Corepack 0.35.0, pi-coding-agent 0.84.1, Shopify CLI 4.6.1, Bun 1.3.14, gh 2.97.0, Mole 1.50.0, and mise 2026.8.3 stayed current. npm 12.0.2 remained skipped as a major migration; Bun has no global package set.
- uv stayed current at 0.12.3. `code-review-graph` stayed at 2.3.7 while only isolated dependencies refreshed; it was touched only with `uv tool upgrade code-review-graph` and verified with `code-review-graph --version`. Voice Mode stayed at 8.12.0 while isolated dependencies refreshed, and pyright stayed at 1.1.411. No CRG install/build/update/postprocess/watch/daemon/embed/server/schema/graph command ran, and Graphify/graphifyy remained untouched.
- MCP package resolution succeeded without starting servers. Claude's floating Context7 4.0.0 and Playwright 0.0.79 resolve; GCP MCP 0.5.3 is current. Existing Codex fixed pins Context7 4.0.0, Serena 1.7.0, and GCP MCP 0.5.3 resolve and required no change. No MCP config, authorization state, secret, or project-scope setting changed. Claude's cached Notion and Slack authorization needs remain manual items.
- Rust stable/rustc/cargo 1.97.1 and rustup 1.29.0 remain current; installed mise runtimes stayed unchanged and no Cargo-installed tools exist. Skipped GUI casks block-buzz `0.5.2 -> 0.5.8`, ngrok `3.39.8 -> 3.39.10`, and Orca `1.4.168 -> 1.4.179`; macOS/Xcode/Simulator; language runtimes OpenJDK/OpenJDK 21/Python 3.13; and non-CLI libraries certifi, fontconfig, harfbuzz, nspr, p11-kit, pango, pgvector, and SDL3.
- Mechanical semantic audit confirms both handoff skills keep the primary checkout's root `MEMORY.md` as their only persistence target. The Codex ship adapter explicitly delegates to `~/.agents/skills/ship/SKILL.md`, but the Claude ship adapter is still missing. Claude Fugu advisor/worker compatibility notes still cite Codex 0.146.0 while 0.147.0 is installed. Tracked README/rules/reference scanning found no new current settings, hook source, CLI version, or plugin enabled-state value promoted into long-term policy. No policy, adapter, or custom-skill rewrite was attempted.
- Final version checks and health checks passed with no unresolved update failure. Restart Codex to load refreshed process state. Next run should use the active NVM Corepack path rather than stale `/usr/local/bin/corepack`; do not auto-upgrade npm 12, GUI casks, language runtimes/libraries, untrusted taps, Graphify, CRG installer/schema/graph work, App-managed marketplaces, project-scope plugins, local/custom skills, the missing Claude ship adapter, or Fugu compatibility text without governing evidence or manual review.

## 2026-08-12T09:11:46+0800 daily development-tool update

- Completed the unattended user-level update from `$HOME` without entering any product repository or the Orca system-automations workspace. No project dependency, `.mcp.json`, graph database, hook, lockfile, repository worktree, secret, OAuth flow, or scheduler configuration was changed, and no MCP server was started.
- Claude Code stayed current at 2.1.228 through `claude update`. `claude doctor` confirms a healthy native install, bundled search, latest-channel auto-updates, and no installation issue. Refreshed all seven configured marketplaces with official commands; their Git heads stayed unchanged. All six user-scope installed marketplace plugins stayed current with enabled/disabled state preserved; project-scope plugin installations were not mutated.
- The mattpocock marketplace stayed at `84fdeffd12f2` with upstream manifest 1.2.3. Before refresh, 23 Matt-managed Codex copies matched old upstream and the local `code-review` description override differed. After refresh, `uv run python ~/.claude/scripts/reconcile_matt_manifest.py --write --runtime` confirmed the self-held manifest and Claude runtime inventory are current at 24 skills; `claude plugin validate` passed. The `code-review` override was preserved as required, all other managed copies still match upstream, and no local/custom skill changed. Upstream HEAD did not change, so no new semantic delta existed to review.
- Codex stayed current at 0.147.0 through its official updater and requested restart. `codex plugin marketplace upgrade` found every configured Git marketplace current; bundled, primary-runtime, curated, and App-managed marketplaces remained runtime/App managed. Codex Doctor reports 0 failures and healthy install/config/auth/network/databases/update state plus ten locally consistent MCP configurations; the existing warning for 417 stale rollout rows remains and no cleanup was attempted.
- Homebrew metadata/runtime updated `6.0.16-2-g007333f -> 6.0.17-8-g50ceebc`, then mise updated `2026.8.3 -> 2026.8.4`. A dry run confirmed mise was the only outdated CLI formula and its transaction was bounded. Homebrew's untrusted-tap protection was not bypassed.
- Voice Mode stayed at 8.12.0 while its isolated `openai` dependency updated `2.53.0 -> 2.54.0`. uv 0.12.3, code-review-graph 2.3.7, and pyright 1.1.411 stayed current. CRG was touched only with `uv tool upgrade code-review-graph` and `code-review-graph --version`; no install/build/update/postprocess/watch/daemon/embed/server/schema/graph command ran, and Graphify/graphifyy remained untouched.
- npm 11.16.0, active NVM Corepack 0.35.0, pnpm 11.21.0, pi-coding-agent 0.84.1, Shopify CLI 4.6.1, Vercel CLI 58.9.1, Bun 1.3.14, gh 2.97.0, Mole 1.50.0, rustup 1.29.0, and Rust/rustc/cargo 1.97.1 stayed current. npm 12.0.2 remained skipped as a major migration. Vercel registry metadata advertises 58.9.4, but the normal pnpm `latest` install retained 58.9.1; no forced version or release-age/protection bypass was used. Bun has no global package set, installed mise runtimes stayed unchanged, and no Cargo-installed tools exist. The shell still resolves stale `/usr/local/bin/corepack` 0.28.0 first; checks used the active NVM Corepack without changing PATH.
- MCP package resolution succeeded without starting servers: floating Context7 4.0.2 and Playwright 0.0.79 resolve, while GCP MCP 0.5.3 and Serena 1.7.0 remain current. Existing fixed pins were left unchanged because no reviewed compatibility change was required. No MCP config, authorization state, secret, or project-scope setting changed. Claude's cached Notion and Slack authorization needs remain manual items.
- Skipped GUI casks block-buzz `0.5.2 -> 0.5.9`, ngrok `3.39.8 -> 3.39.10`, and Orca `1.4.168 -> 1.4.180`; macOS/Xcode/Simulator; language runtimes OpenJDK/OpenJDK 21/Python 3.13; and non-CLI libraries certifi, expat, fontconfig, harfbuzz, nspr, p11-kit, pango, pgvector, and SDL3.
- Mechanical semantic audit confirms both handoff skills keep the primary checkout's root `MEMORY.md` as their only persistence target, and the Codex ship adapter explicitly delegates to `~/.agents/skills/ship/SKILL.md`. Manual items remain: the Claude ship adapter is missing; Claude Fugu advisor/worker compatibility notes still cite Codex 0.146.0; and the Matt-managed Codex `code-review` skill has an intentional local description override. Tracked README/rules/reference scanning found no current settings, hook source, CLI version, or plugin enabled-state value promoted into long-term policy. No policy, adapter, or custom-skill rewrite was attempted.
- Final version and health checks passed with no unresolved update failure. Restart Codex to load refreshed process state. Next run should not auto-upgrade npm 12, force Vercel past pnpm's normal resolution, change the stale system Corepack path, mutate MCP fixed pins, touch GUI casks/language runtimes/libraries/untrusted taps, install Graphify, run CRG installer/schema/graph work, update App-managed or project-scope plugins, or rewrite local/custom skills, ship adapters, Fugu compatibility text, or the preserved `code-review` override without governing evidence or manual review.

## 2026-08-13T12:24:26+0800 daily development-tool update

- Completed the unattended user-level update from `$HOME` without entering any product repository or the Orca system-automations workspace. No project dependency, `.mcp.json`, graph database, graph hook, lockfile, repository worktree, secret, OAuth state, or scheduler configuration was changed.
- Claude Code stayed current at 2.1.229 through `claude update`. `claude doctor` confirms a healthy native install, bundled search, latest-channel auto-updates, and no installation issue. Refreshed all seven configured marketplaces with official commands; all observed Git heads stayed unchanged. All six user-scope marketplace plugins stayed current with enabled/disabled state preserved; three project-scope plugin installations were not mutated. Codex Git marketplaces were already current, while bundled, primary-runtime, curated, and App-managed marketplaces remained runtime/App managed.
- The mattpocock marketplace stayed at `84fdeffd12f2` with upstream manifest 1.2.3. Before refresh, 22 Matt-managed Codex copies matched the old upstream; `to-tickets` and `code-review` had local differences and were preserved. `uv run python ~/.claude/scripts/reconcile_matt_manifest.py --write --runtime` confirmed the self-held manifest and Claude runtime inventory are current at 24 skills; `claude plugin validate` passed, the marketplace skill symlink remains valid, and the managed name set equals `desired = upstream - handoff`. No upstream HEAD or semantic-contract delta existed to review, and no local/custom skill was rewritten.
- Homebrew metadata/runtime updated `6.0.17-13-g0d7b47e -> 6.0.17-27-gec55b2e`. Dry runs confirmed bounded transactions, then mise updated `2026.8.4 -> 2026.8.5` and Supabase CLI `2.113.0 -> 2.114.0`. Homebrew's untrusted-tap protection was not bypassed.
- Updated pnpm-global Vercel CLI `58.9.1 -> 58.9.4` through the normal `latest` resolution. Registry metadata advertised 58.10.0, but pnpm's existing publication-age policy selected 58.9.4; no forced version or protection bypass was used. npm 11.16.0, active NVM Corepack 0.35.0, pnpm 11.21.0, pi-coding-agent 0.84.1, Shopify CLI 4.6.1, Bun 1.3.14, gh 2.97.0, Mole 1.50.0, and usage 5.1.0 stayed current. npm 12.0.2 remained skipped as a major migration; stale `/usr/local/bin/corepack` 0.28.0 remains outside the active NVM path.
- uv stayed current at 0.12.3. `code-review-graph` stayed at 2.3.7 while its isolated `typing-inspection` dependency updated `0.4.3 -> 0.4.4`; it was touched only with `uv tool upgrade code-review-graph` and `code-review-graph --version`. Voice Mode stayed at 8.12.0 while its isolated environment updated `openai 2.54.0 -> 3.0.0`, added httpcore2/httpx2 2.10.0 and truststore 0.10.4, and refreshed typing-inspection; its version check passed. Pyright stayed at 1.1.411. No CRG install/build/update/postprocess/watch/daemon/embed/server/schema/graph command ran, and Graphify/graphifyy remained untouched.
- rustup 1.29.0 stayed current through its self-updater; Rust/rustc/cargo stayed at 1.97.1. Installed mise runtimes were unchanged, and no Cargo-installed tools or Bun globals exist.
- MCP registry resolution is current without a config change: floating Context7 4.0.2 and Playwright 0.0.79 resolve, GCP MCP 0.5.3 and Serena 1.7.0 are current, and the disabled fixed Context7 4.0.0 pin was left unchanged pending reviewed compatibility evidence. Claude still reports Notion and Slack need authentication. No secret or project-scope MCP setting changed. A `claude mcp list` inventory health probe was run and may have transiently spawned configured stdio health checks. Multiple MCP processes already exist on the machine, but their ownership cannot be safely attributed across active Orca/Codex sessions, so none was terminated or otherwise managed. Future runs must use registry/config inspection only to honor the no-start constraint strictly.
- Codex stayed current at 0.147.0, but `codex update` failed because the updater could not detect the standalone installation method. Official npm metadata and Codex Doctor both report 0.147.0 as latest; no alternate installer was used. Codex Doctor completed with 0 failures and healthy install/config/auth/network/database/update/MCP state. Existing warnings now include 1,977 rollout files missing from the thread state DB and unsupported project-local config keys; no cleanup or project config edit was attempted.
- Skipped GUI casks block-buzz `0.5.2 -> 0.5.11` and ngrok `3.39.8 -> 3.39.11`; macOS/Xcode/Simulator; language runtimes OpenJDK/OpenJDK 21/Python 3.13; FFmpeg `8.1.2_1 -> 9.0.1` as a major migration; and non-CLI libraries certifi, expat, fontconfig, gdk-pixbuf, harfbuzz, nspr, p11-kit, pango, pgvector, and SDL3.
- Mechanical semantic audit confirms the only canonical ship skill is `~/.agents/skills/ship/SKILL.md`, and both handoff skills keep the primary checkout's root `MEMORY.md` as their only persistence target. No Claude or Codex ship adapter/reference was found in the inspected user-level runtime paths; this differs from the previous memo's Codex-adapter observation and remains a manual policy decision. Claude Fugu advisor/worker compatibility notes still cite Codex 0.146.0. Matt-managed `to-tickets` has a local handoff-boundary addition and `code-review` has a local description override; both remain manual-review items. Tracked README/rules/reference scanning found no current settings, hook source, CLI version, or plugin enabled-state value promoted into long-term policy.
- Final version checks passed for all installed CLIs. Next run should not retry npm 12, force Vercel past pnpm's normal resolution, change the stale system Corepack path, mutate MCP fixed pins, run MCP health/list commands that may start stdio checks, touch GUI casks/language runtimes/libraries/untrusted taps, install Graphify, run CRG installer/schema/graph work, update App-managed or project-scope plugins, or rewrite local/custom skills, ship adapters, Fugu compatibility text, `to-tickets`, or `code-review` without governing evidence or manual review. The Codex updater installation-method detection failure remains reproducible but does not leave Codex outdated.

## 2026-08-13T12:37:22+0800 Matt official restore follow-up

- Refreshed the mattpocock marketplace with the official command and confirmed it is current at HEAD `84fdeffd12f2` and manifest version 1.2.3.
- Per explicit user direction, removed the local Matt-managed overrides from `to-tickets` and `code-review` by restoring both directories exactly from the marketplace clone. The previously recorded manual-review items for these two overrides are now resolved.
- Ran `uv run python ~/.claude/scripts/reconcile_matt_manifest.py --write --runtime` and `claude plugin validate ~/.claude/skills/mattpocock-skills`; manifest/runtime inventory remain current at 24 desired skills and validation passed.
- Verified all 24 Matt-managed copies under `~/.agents/skills/` match official upstream byte-for-byte. The excluded local `handoff` skill and every non-Matt custom skill remained untouched.

## 2026-08-13T12:54:57+0800 Fugu adapter compatibility repair

- Replaced the version-targeted `~/.local/bin/codex-fugu` bundle launcher with one capability-driven transport. It no longer compares, installs, upgrades, or downgrades Codex versions and no longer depends on a named Fugu profile. Each preflight discovers the current Codex binary, required exec flags, apps feature, MCP JSON contract, model catalog, and credential source, then asks `codex debug prompt-input` to parse the complete Fugu configuration without a provider request.
- The launcher now supports zero-request `preflight`/`--status`, unattended `run --mode advisor|worker`, task-local event/report paths, and same-session `--resume`. It selects Fugu Ultra for skill dispatch, dynamically disables every configured MCP plus apps, enforces `approval_policy=never`, and selects read-only or workspace-write sandbox by mode. Credential values are never printed; the named key is accepted from the process environment or loaded from the active Codex home/legacy `~/.codex/.env` locations.
- Updated the Codex and Claude copies of `fugu-advisor` and `fugu-worker` to use only this launcher. Removed Codex's unavailable `spawn_agent(agent_type=...)` path and every Codex-version compatibility claim or gate from the four skills. Their explicit-invocation policy and one-call/no-substitute contract remain intact.
- Added deterministic fake-Codex coverage at `~/.local/share/codex-fugu/test_codex_fugu.py`. Five tests pass for arbitrary future version strings, worker isolation flags, advisor read-only/resume, missing credentials, and missing model catalog entries. Python compilation, both Codex skill validations, Claude frontmatter parsing, Claude plugin inventory, and a live zero-request preflight passed on Codex 0.147.0; the live preflight resolved the catalog, credential source, complete Fugu config, and nine MCP servers.
- No Sakana/Fugu model request was sent during repair or validation, so provider authentication, quota, latency, and answer quality remain deliberately untested until the next explicit `$fugu-advisor` or `$fugu-worker` invocation. Future maintenance should run `codex-fugu preflight` and the fake-Codex suite instead of comparing CLI versions or restoring the removed bundle-deployment logic.

## 2026-08-13T13:08:58+0800 Fugu legacy cleanup follow-up

- Per explicit user direction, removed the obsolete bundle-deployment state, generated version-pinned configs, retired agent profiles, and the clean upstream Fugu clone from their active user-level paths. The six exact targets were moved, with their relative layout preserved, to `~/.Trash/fugu-legacy-2026-08-13T1255+0800`; this cleanup remains recoverable until Trash is emptied.
- Preserved the capability-driven launcher and tests, current model catalog, credential source, Sakana provider configuration, and all four Fugu skills. A post-cleanup live zero-request preflight passed on Codex 0.147.0, and all five fake-Codex adapter tests passed. No Sakana/Fugu model request was sent.

## 2026-08-13T13:20:05+0800 Corepack cleanup attempt

- Confirmed that the active NVM Node 24.18.0 installation already contains Corepack 0.35.0 and valid `pnpm`, `pnpx`, `yarn`, and `yarnpkg` shims, while the obsolete root-owned `/usr/local` Corepack 0.28.0 shadows it in inherited PATHs. An npm prefix-scoped dry run showed that uninstalling `corepack` from `/usr/local` would remove only the obsolete package.
- The exact prefix-scoped uninstall was attempted with non-interactive sudo but skipped because no cached administrator credential exists. Both root-owned targets remain unchanged, and their parent directories are not writable by the current account. Do not add an alias, duplicate install, or PATH workaround; the remaining clean action requires a human-admin execution of the prefix-scoped npm uninstall, followed by `rehash` and version/path verification.

## 2026-08-13T13:22:06+0800 Corepack cleanup completed

- The user completed the administrator-authorized prefix-scoped uninstall. Verified that the obsolete `/usr/local/bin/corepack` link and `/usr/local/lib/node_modules/corepack` package are absent. Corepack now resolves only through the active NVM Node 24.18.0 installation at version 0.35.0, while pnpm remains on the same NVM path at 11.21.0. The prior sudo blocker is resolved; no alias or PATH workaround was added.

## 2026-08-13T13:25:52+0800 MCP OAuth decision

- Per explicit user direction, skipped interactive reauthorization for the `claude.ai Notion` and `claude.ai Slack` connectors. Do not treat these connector authentication states as unattended maintenance failures or retry them automatically; revisit only on a future explicit user request.

## 2026-08-13T13:34:07+0800 explicitly authorized deferred upgrades

- Per explicit user direction, expanded this follow-up to the items previously deferred by the unattended maintenance policy. Homebrew upgraded all 14 outstanding core formulae through official bottles: certifi `2026.6.17 -> 2026.7.22`, expat `2.8.2 -> 2.8.3`, FFmpeg `8.1.2_1 -> 9.0.1`, fontconfig `2.18.2 -> 2.18.3`, gdk-pixbuf `2.44.7 -> 2.44.8`, harfbuzz `14.2.1 -> 14.3.1`, nspr `4.39 -> 4.40`, OpenJDK `26.0.1 -> 26.0.2`, OpenJDK 21 `21.0.11 -> 21.0.12`, p11-kit `0.26.4 -> 0.26.5`, pango `1.58.0 -> 1.58.2`, pgvector `0.8.5 -> 0.8.6`, Python 3.13 `3.13.14_1 -> 3.13.15`, and SDL3 `3.4.12 -> 3.4.14`.
- All 14 upgraded formulae passed `brew linkage --test`; FFmpeg, both JDKs, and Python passed direct runtime checks. `brew missing` is empty and the final Homebrew outdated inventory contains no formulae or casks. `brew doctor` reports only the pre-existing six untrusted taps, which remained ignored; no tap was trusted and the protection was not bypassed.
- Upgraded the active NVM default Node `24.18.0 -> 24.19.0` and npm `11.16.0 -> 12.0.2` after confirming npm's engine support. Migrated the same four global package names, then verified pi-coding-agent 0.84.1, Shopify CLI 4.6.1, Corepack 0.35.0, and pnpm 11.21.0. The final `npm doctor` passed every check. The prior Node 24.18.0 installation remains available for rollback or an explicit `.nvmrc`; it is no longer the default.
- A single root-owned npm debug log created by the earlier sudo attempt was moved recoverably to `~/.Trash/npm-root-log-2026-08-13T052146Z-debug-0.log`; no other npm cache file was manually changed. npm's own doctor garbage-collected stale cache content during verification.
- Upgraded GUI casks Block Buzz `0.5.2 -> 0.5.11` and ngrok `3.39.8 -> 3.39.11` after confirming neither target was running. Homebrew performed its normal app/binary replacement and old-cask purge; the Buzz app reports 0.5.11 and passes strict code-signature verification, while `ngrok version` reports 3.39.11.
- No project repository, project dependency, lockfile, MCP configuration, OAuth state, graph state, untrusted tap setting, or user skill was changed during this follow-up. The explicit decision to skip Notion and Slack reauthorization remains in effect.

## 2026-08-13T13:45:52+0800 daily prompt optimization

- Reworked the daily agent document into a five-stage process with checkable completion criteria, one canonical memory read, bounded retry behavior, actual binary-path baselines, same-major update boundaries, install-source-aware Codex handling, config-only MCP inspection, and concise checkpoint writes. Cached marketplace names and version snapshots were removed.
- Moved Matt ownership and semantic-delta handling behind `~/.claude/automations/daily-dev-update/MATT-SYNC.md`. Unchanged HEAD/manifest runs now take a mechanical fast path; per-skill synchronization and semantic review run only on an observed upstream delta. Current Matt manifest/runtime checks and Claude plugin validation passed at 24 skills.
- This edit changes only the user-level execution instructions and memory. The Orca automation configuration, schedule, workspace context, provider launch profile, and existing run records remain unchanged; startup-time MCP loading and pre-dispatch network gating still require Orca-level support rather than prompt text.

## 2026-08-14T09:09:29+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`. No product repository, Orca workspace, project dependency, lockfile, `.mcp.json`, graph state, hook, OAuth state, secret, or scheduler configuration was read or changed, and no MCP server was started.
- Updated Homebrew metadata/runtime `6.0.17-27-gec55b2e -> 6.0.17-36-g6cf9b12`, then upgraded `ca-certificates 2026-07-16 -> 2026-08-13`, `libpq 18.4 -> 18.6`, `nss 3.126 -> 3.126.1`, and `python@3.14 3.14.6 -> 3.14.7` after a bounded dry run. All four passed linkage checks; PostgreSQL and Python runtime checks passed, the CA bundle exists, and `brew missing` is empty. Untrusted taps remained ignored.
- Updated `uv 0.12.3 -> 0.12.4`. `code-review-graph` stayed at 2.3.7 while its isolated `platformdirs 4.11.2 -> 4.11.3` and `uvicorn 0.52.1 -> 0.52.3` dependencies refreshed; Voice Mode stayed at 8.12.0 with the same dependency refresh plus isolated `uv 0.12.3 -> 0.12.4`; Pyright stayed at 1.1.411. CRG was touched only with `uv tool upgrade code-review-graph` and `code-review-graph --version`; no installer, schema, graph, daemon, server, embed, build, update, postprocess, or hook command ran, and Graphify/graphifyy remain absent.
- Refreshed all seven Claude marketplaces and every user-scope marketplace plugin with official commands. `anthropic-agent-skills f17010c9bb48 -> f6656c1256d5` and `mattpocock 84fdeffd12f2 -> 8b78b531ab96`; other observed heads, plugin versions, scopes, and enabled states were unchanged. The Matt delta only broadened the `domain-modeling` trigger description, with no completion, commit, publish, or external-write semantic change. All 24 pre-refresh copies matched old upstream, the one affected copy was synchronized, all 24 now match new upstream, reconcile/runtime inventory passed, and Claude plugin validation passed.
- Claude Code remains current and healthy at 2.1.232 with native latest-channel updates. Codex standalone remains current at 0.147.0; the known installation-method updater failure was not retried because install source, current version, and official latest are unchanged. Codex Git marketplaces are current. Codex Doctor reports 0 failures and the existing 417 stale thread rows; no cleanup was attempted.
- Config-only MCP registry audit resolved floating Context7 4.0.2, Playwright 0.0.79, and Chrome DevTools MCP 1.7.0; fixed GCP 0.5.3 and Serena 1.7.0 are current. Disabled fixed Context7 4.0.0 remains held while latest is 4.0.2 because no reviewed migration evidence exists. No health/list command started a configured server. The explicit Notion and Slack no-reauthorization decision remains in effect.
- Node 24.19.0 is still the NVM default and latest Node 24; npm 12.0.2, Corepack 0.35.0, pnpm 11.21.0, Bun 1.3.14, gh 2.97.0, Mole 1.50.0, mise 2026.8.5, rustup 1.29.0, and Rust/rustc/cargo 1.97.1 remain current. Global package names and enabled plugin states are unchanged. The unattended PATH does not resolve Rust commands, but the existing `~/.cargo/bin` installation passed official checks by absolute path.
- Deferred Orca GUI cask `1.4.168 -> 1.4.182` and Vercel CLI `58.9.4 -> 59.0.0` because GUI casks and major CLI migrations require an explicit decision; pnpm's allowed same-major target remains 58.9.4. Do not retry these, fixed Context7, Notion/Slack OAuth, untrusted taps, Codex updater, Graphify, CRG graph/schema work, or stale-row cleanup unless their version, reason, governing evidence, or user decision changes.

## 2026-08-15T11:06:56+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`. No product repository, Orca workspace, project dependency, lockfile, `.mcp.json`, graph state, hook, OAuth state, secret, or scheduler configuration was read or changed, and no MCP server was started.
- Refreshed Homebrew metadata `6cf9b128d3 -> 38ee3253f5` and upgraded mise `2026.8.5 -> 2026.8.6` through the bounded same-major formula transaction. `brew linkage --test mise` and `brew missing` passed; untrusted taps remained ignored.
- Updated the global npm CLI `@earendil-works/pi-coding-agent 0.84.1 -> 0.84.2` and uv `0.12.4 -> 0.12.5`. The existing npm install-script allowlist remained in force; blocked transitive scripts were not enabled. `pi --version` and `uv --version` passed. `code-review-graph` remained `2.3.7`; its permitted upgrade command reported nothing to upgrade and no graph/install/schema command ran.
- Claude Code remained current at `2.1.233` from its native install. `claude doctor` reports native/latest-channel auto-updates enabled and no installation issues. All seven configured marketplaces were refreshed with official commands; observed Git heads, user-scope plugin versions, and enabled/disabled states were unchanged. The addy-web-quality-skills refresh did not return its final completion line; its clean HEAD remained unchanged and no retry was made under the single-attempt rule.
- Codex remained current at standalone `0.147.0`; official registry metadata and Codex Doctor agree that this is latest. The known standalone updater installation-method failure was not retried because install source, current version, and latest version were unchanged. Codex Doctor completed with 0 failures; the existing rollout/state inventory warning remains (2,344 active files, 2,037 missing active rows) and no cleanup was attempted. Codex Git marketplaces were current.
- The Matt marketplace took the fast path at unchanged HEAD `8b78b531ab96`. The self-held manifest/runtime inventory remains current at 24 skills, symlink and marketplace validation passed, and the pre-existing local delta in `code-review` was preserved for manual review.
- Config-only MCP auditing resolved floating Context7 `4.0.2`, Playwright `0.0.79`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current. The disabled fixed Context7 `4.0.0` pin remains held without reviewed migration evidence. No MCP server was started or health-probed. The explicit decision to skip Notion and Slack reauthorization remains in effect.
- Node `24.19.0` remains the NVM default/latest Node 24; npm `12.0.2`, Corepack `0.35.0`, pnpm `11.21.0`, Bun `1.3.14`, gh `2.97.0`, Mole `1.50.0`, rustup `1.29.0`, and Rust/rustc/cargo `1.97.1` remain current and passed runtime/version checks. Global package-name sets and plugin states are unchanged. Rust checks used the absolute `~/.cargo/bin` paths because Rust is not on the unattended PATH.
- Skipped Orca GUI cask `1.4.168 -> 1.4.182` because GUI casks require an explicit decision, and Vercel CLI `58.9.4 -> 59.0.0` because it is a major migration. Next retry keys: `addy-marketplace-final-output`, `matt-code-review-local-delta`, `codex-updater-install-method`, `context7-fixed-pin`, `orca-gui-cask`, and `vercel-major` only when the observed version, reason, evidence, or user decision changes.

## 2026-08-16T11:49:36+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth, secret, scheduler configuration, or uncommitted work was read or changed, and no MCP server started.
- Refreshed Homebrew metadata `38ee3253f5 -> 6c0fcdcdd7` and updated Corepack-managed pnpm `11.21.0 -> 11.22.0`. No formula was outdated; global package-name sets stayed unchanged and runtime checks passed.
- Claude Code stayed current/healthy at native `2.1.233`. Refreshed all seven marketplaces and six user-scope marketplace plugins while preserving scope/enabled state. Matt advanced `8b78b531ab96 -> 068b6e0c6239`; 23 old-matching managed copies now match new upstream, while the broad Graph-specific `code-review` local delta was preserved. Manifest/runtime, plugin, canonical ship/thin adapter, and identical root-`MEMORY.md` handoff checks passed. The upstream invocation/completion semantic delta was recorded without rewriting local policy.
- Config-only MCP audit resolved floating Context7 `4.0.2`, Playwright `0.0.79`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current. Disabled fixed Context7 `4.0.0` remains held; Notion/Slack reauthorization remains explicitly skipped. CRG stayed `2.3.7` through its sole permitted upgrade command; no MCP list/health, graph/schema/server, or Graphify command ran.
- Codex standalone stayed current at `0.147.0`; official metadata and Doctor agree, so the unchanged known updater failure was not retried. Git marketplaces are current. Doctor passed with 0 failures; existing warnings remain for 2,194 rollout files missing active DB rows and unsupported project-local config keys, with no cleanup/config edit.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, Bun `1.3.14`, gh `2.97.0`, Mole `1.50.0`, mise `2026.8.6`, uv `0.12.5`, Pyright `1.1.411`, Voice Mode `8.12.0`, rustup `1.29.0`, and Rust/rustc/cargo `1.97.1` remain current. `brew missing`, trust protection, path/version, plugin-state, and credential presence/source checks passed without exposing secrets.
- Retry only on changed evidence or explicit direction: `matt-code-review-local-delta`, `context7-fixed-pin`, Vercel major `58.9.4 -> 59.1.3`, Block Buzz GUI `0.5.11 -> 0.5.14`, `codex-updater-install-method`, and Notion/Slack OAuth. Untrusted taps, App-managed caches, GUI casks, project-scope plugins, local/custom skills, and prohibited graph work remained untouched.

## 2026-08-17T09:38:03+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth, secret, scheduler configuration, or uncommitted work was read or changed, and no MCP server started.
- Refreshed Homebrew metadata `6c0fcdcdd7 -> fba9b30d2b` and upgraded Mole `1.50.0 -> 1.51.0`. Mole linkage/runtime checks and `brew missing` passed; no formula remains outdated, and untrusted taps remained ignored.
- `code-review-graph` stayed at `2.3.7` while its isolated dependencies updated: `griffelib 2.1.0 -> 2.2.0` and `python-dotenv 1.2.2 -> 1.2.3`. Only `uv tool upgrade code-review-graph` and `code-review-graph --version` were used; no installer, schema, graph, daemon, server, embed, build, update, postprocess, hook, or Graphify command ran.
- Claude Code stayed current/healthy at native `2.1.233`. Refreshed all seven marketplaces and six user-scope marketplace plugins while preserving scope/enabled state. Matt took the fast path at unchanged HEAD `068b6e0c6239`; manifest/runtime inventory and plugin validation passed for 24 skills, and the existing `code-review` local delta remained untouched.
- Codex standalone stayed current at `0.147.0`; official registry metadata and Doctor agree, so the unchanged known updater failure was not retried. Git marketplaces are current. Doctor passed with 0 failures; existing warnings remain for 2,194 rollout files missing active DB rows and unsupported project-local config keys, with no cleanup/config edit.
- Config-only MCP registry audit resolved floating Context7 `4.0.2`, Playwright `0.0.79`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current. Disabled fixed Context7 `4.0.0` remains held, and Notion/Slack reauthorization remains explicitly skipped. No MCP list/health or server-starting command ran.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.22.0`, Bun `1.3.14`, gh `2.97.0`, mise `2026.8.6`, uv `0.12.5`, Pyright `1.1.411`, Voice Mode `8.12.0`, rustup `1.29.0`, and Rust/rustc/cargo `1.97.1` remain current. Global package names, plugin states, and credential presence/source checks passed without exposing secrets.
- Retry only on changed evidence or explicit direction: `matt-code-review-local-delta`, `context7-fixed-pin`, Vercel major `58.9.4 -> 59.1.3`, Block Buzz GUI `0.5.11 -> 0.5.14`, `codex-updater-install-method`, and Notion/Slack OAuth. App-managed caches, GUI casks, project-scope plugins, local/custom skills, untrusted taps, and prohibited graph work remained untouched.

## 2026-08-18T09:12:14+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth, secret, scheduler configuration, or uncommitted project work was read or changed, and no MCP server started.
- Claude Code advanced through its healthy native auto-update before this run `2.1.233 -> 2.1.234`. Refreshed all seven Claude marketplaces and six user-scope marketplace plugins while preserving scope/enabled state. Matt advanced `068b6e0c6239 -> 9c9f36ccd399`; the only upstream delta removed one explanatory section from `docs/engineering/grill-with-docs.md`, with no managed-skill or completion/external-write semantic change. Manifest/runtime inventory and real-skill validation passed for 24 skills; the existing `code-review` local delta remains preserved.
- Updated Homebrew `6.0.17-97-gfba9b30 -> 6.0.18-16-gb5d22b1`, mise `2026.8.6 -> 2026.8.8`, NSS `3.126.1 -> 3.127`, and Ruby formula revision `4.0.6 -> 4.0.6_1`. Exact dry-run, all three linkage checks, Ruby runtime, and `brew missing` passed; no formula remains outdated and untrusted taps stayed ignored.
- `code-review-graph` stayed at `2.3.7` while isolated cyclopts `4.22.5 -> 4.23.0` and Pygments `2.20.0 -> 2.21.0` updated. Voice Mode stayed at `8.12.0` while its isolated cyclopts, griffelib `2.1.0 -> 2.2.0`, OpenAI `3.0.0 -> 3.2.0`, Pygments, python-dotenv `1.2.2 -> 1.2.3`, sounddevice `0.5.5 -> 0.5.6`, and uv `0.12.4 -> 0.12.5` updated. Pyright stayed at `1.1.411`. CRG used only its permitted uv upgrade/version commands; no graph, schema, installer, server, daemon, embed, hook, or Graphify command ran.
- Codex standalone stayed current at `0.147.0`; official registry metadata and Doctor agree, so the unchanged known updater failure was not retried. Git marketplaces are current. Doctor passed with 0 failures; the existing 417 stale rollout rows remain and no cleanup ran.
- Config-only MCP audit resolved floating Context7 `4.0.2`, Playwright `0.0.79`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current. Disabled fixed Context7 `4.0.0` remains held without reviewed migration evidence. Notion/Slack reauthorization remains explicitly skipped; no MCP list/health/server command ran.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.22.0`, Bun `1.3.14`, gh `2.97.0`, Mole `1.51.0`, uv `0.12.5`, rustup `1.29.0`, and Rust/rustc/cargo `1.97.1` remain current. Installed inactive mise runtimes are not outdated; global package-name sets, user plugin states, runtime paths, and credential presence/source checks passed without exposing secrets.
- Retry only on changed evidence or explicit direction: `matt-code-review-local-delta`, `context7-fixed-pin`, Vercel major `58.9.4 -> 59.1.4`, Block Buzz GUI `0.5.11 -> 0.5.14`, `codex-updater-install-method`, and Notion/Slack OAuth. App-managed caches, GUI casks, project-scope plugins, local/custom skills, untrusted taps, and prohibited graph work remained untouched.

## 2026-08-19T10:47:40+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth, secret, scheduler configuration, or uncommitted project work was entered or changed, and no MCP server started.
- Updated Codex standalone `0.147.0 -> 0.148.0` through its official installer because the official latest changed. Git marketplaces are current. Codex Doctor passed with 0 failures; the existing 417 stale rollout rows remain and no cleanup ran. Restart Codex to load the new process state.
- Claude Code stayed current/healthy at native `2.1.235`. Refreshed all seven configured marketplaces and all six user-scope marketplace plugins while preserving scope/enabled state; `openai-codex` omitted its terminal completion line, but its clean local HEAD equals remote HEAD and its plugin is current. Matt took the fast path at unchanged HEAD `9c9f36ccd399`; manifest/runtime inventory passed for 24 skills and the existing `code-review` local delta remained untouched.
- Refreshed Homebrew metadata `b5d22b118c -> a2005e53de`, upgraded OpenJDK 21 `21.0.12 -> 21.0.12.1`, Redis `8.10.0 -> 8.10.1`, Supabase CLI `2.114.0 -> 2.115.0`, and tmux `3.7b -> 3.7c`, and installed tmux dependency jemalloc `5.3.1`. Exact dry-run, all five linkage checks, direct runtime checks, and `brew missing` passed; no formula remains outdated and untrusted taps stayed ignored. The installed Redis binary is `8.10.1`, but the running service remains `8.10.0`; it was not restarted because that can interrupt local workloads.
- `code-review-graph` stayed at `2.3.7` while isolated idna updated `3.18 -> 3.19`. Voice Mode stayed at `8.12.0` while isolated httpcore2/httpx2 updated `2.10.0 -> 2.12.0`, idna `3.18 -> 3.19`, OpenAI `3.2.0 -> 3.3.0`, and unused tqdm `4.70.0` was removed. Pyright stayed at `1.1.411`. CRG used only its permitted uv upgrade/version commands; no graph, schema, installer, server, daemon, embed, hook, or Graphify command ran.
- Config-only MCP registry audit resolved floating Context7 `4.0.2`, Playwright `0.0.79`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current. Disabled fixed Context7 `4.0.0` remains held without reviewed migration evidence. Notion/Slack reauthorization remains explicitly skipped; no MCP list/health/server command ran.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.22.0`, Bun `1.3.14`, gh `2.97.0`, Mole `1.51.0`, mise `2026.8.8`, uv `0.12.5`, rustup `1.29.0`, and Rust/rustc/cargo `1.97.1` remain current. Global package names, user plugin states, runtime paths, installed mise runtimes, and credential presence/source checks passed without exposing secrets.
- Retry only on changed evidence or explicit direction: `redis-service-restart`, `matt-code-review-local-delta`, `context7-fixed-pin`, Vercel major `58.9.4 -> 59.1.4`, Block Buzz GUI `0.5.11 -> 0.5.17`, Notion/Slack OAuth, and Codex stale-row cleanup. App-managed caches, GUI casks, project-scope plugins, local/custom skills, untrusted taps, and prohibited graph work remained untouched.

## 2026-08-20T10:05:16+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth, secret, scheduler configuration, or uncommitted project work was entered or changed, and no MCP server started.
- Updated Claude Code native `2.1.236 -> 2.1.237`; final `claude doctor` confirms the latest-channel install, bundled search, and auto-update state are healthy. Refreshed all seven Claude marketplaces; `marketingskills` advanced `7868cb9251fa -> c6ea12834be6`. An official refresh briefly advanced a project-scope Shopify cache registration, but the exact `1.6.1` baseline registration and all project scopes/enabled states were restored without reading or changing a product repo; App-managed cache files were not deleted or edited.
- Matt advanced `9c9f36ccd399 -> 885e2ca4d842`. Synced all 23 old-matching managed copies, preserved the existing `code-review` local delta byte-for-byte, and kept the 24-skill self-held manifest/symlink current. Runtime inventory and marketplace/plugin validation passed. The upstream change was predominantly grammar/punctuation and added minor clarity; no `disable-model-invocation`, completion, commit, publish, or external-write contract changed.
- Refreshed Homebrew metadata `a2005e53de -> 5df565fc5f`, then upgraded Herdr `0.8.0 -> 0.8.2`, mise `2026.8.8 -> 2026.8.9`, and OpenJDK `26.0.2 -> 26.0.2.1` through the exact dry-run transaction. All linkage/runtime checks and `brew missing` passed; no formula remains outdated and tap trust was not relaxed.
- Updated the effective Shopify CLI `4.6.1 -> 4.7.0` for both its user wrapper target and the NVM default global package. The npm install-script allowlist stayed enforced and blocked esbuild's transitive postinstall; the CLI runtime passed. Global package-name sets stayed unchanged.
- `code-review-graph` stayed `2.3.7` while isolated uvicorn updated `0.52.3 -> 0.52.4`. Voice Mode stayed `8.12.0` while OpenAI updated `3.3.0 -> 3.3.1`, uvicorn updated `0.52.3 -> 0.52.4`, and unused distro was removed; Pyright stayed `1.1.411`. CRG used only its permitted uv upgrade/version commands; no graph, schema, installer, server, daemon, embed, hook, or Graphify command ran.
- Config-only MCP registry audit resolved floating Context7 `4.0.2`, Playwright `0.0.79`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current. Disabled fixed Context7 `4.0.0` remains held without reviewed migration evidence. Notion/Slack reauthorization remains explicitly skipped; no MCP list/health/server command ran.
- Codex standalone `0.148.0`, Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.22.0`, Bun `1.3.14`, gh `2.97.0`, Mole `1.51.0`, uv `0.12.5`, rustup `1.29.0`, and Rust/rustc/cargo `1.97.1` remain current. Codex Git marketplace HEAD equals remote; Doctor passed with 0 failures and the existing 417 stale rollout rows remain untouched. The prior Redis service retry key is closed because both binary and running service now report `8.10.1`.
- Retry only on changed evidence or explicit direction: `matt-code-review-local-delta`, `context7-fixed-pin`, Vercel major `58.9.4 -> 59.1.4`, Block Buzz GUI `0.5.11 -> 0.5.17`, Orca GUI `1.4.168 -> 1.4.185`, Notion/Slack OAuth, and Codex stale-row cleanup. Project-scope plugins, local/custom skills, untrusted taps, App-managed caches, GUI casks, and prohibited graph work remain otherwise untouched.
## 2026-08-21T10:16:16+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or uncommitted project work was entered or changed, and no MCP server started.
- Claude Code had advanced through its healthy native auto-update before the run `2.1.237 -> 2.1.238`. Refreshed all eight configured Git marketplaces and seven user-scope marketplace plugins while preserving scope and enabled state; `diagram-design` updated `2.6.0 -> 2.6.1`.
- Matt advanced `885e2ca4d842 -> 0ab1b63a410a`. Synced the old-matching `grilling` copy; the delta only clarified multi-question round formatting and did not change completion, commit, publish, external-write, or `disable-model-invocation` contracts. Manifest/runtime and marketplace validation passed for 24 skills; 23 copies match upstream and the existing `code-review` local delta remains preserved.
- Updated Codex standalone `0.148.0 -> 0.149.0` through the official standalone installer after the built-in updater could not detect its installation method. Git marketplaces are current. Doctor passed with 0 failures; existing missing rollout/state DB rows remain and no cleanup ran. Restart Codex to load the new process state.
- Refreshed Homebrew metadata `5df565fc5f -> ac9e7acf4e`, then upgraded core formulae gh `2.97.0 -> 2.98.0` and mise `2026.8.9 -> 2026.8.10` from the exact dry-run. Linkage and `brew missing` passed; no formula remains outdated and untrusted-tap protection stayed enabled.
- Updated Bun `1.3.14 -> 1.4.0` through its official self-updater and Rust stable/rustc/cargo `1.97.1 -> 1.98.0` through rustup. Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.22.0`, Mole `1.51.0`, uv `0.12.5`, Pyright `1.1.411`, and Voice Mode `8.12.0` remain current; global package-name sets are unchanged.
- `code-review-graph` stayed `2.3.7` while its isolated cyclopts updated `4.23.0 -> 4.23.1`; Voice Mode received the same isolated dependency update. CRG used only `uv tool upgrade code-review-graph` and `code-review-graph --version`; no installer, graph, schema, server, daemon, embed, hook, or Graphify command ran.
- Config-only MCP auditing resolved floating Context7 `4.0.2`, Playwright `0.0.79`, llm-wiki `0.2.0`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current. Disabled fixed Context7 `4.0.0` remains held, and Notion/Slack reauthorization remains explicitly skipped. No MCP list/health/server command ran.
- Retry only on changed evidence or explicit direction: `matt-code-review-local-delta`, `context7-fixed-pin`, Vercel major `58.9.4 -> 59.3.0`, Block Buzz GUI `0.5.11 -> 0.5.17`, Notion/Slack OAuth, and Codex rollout-state cleanup. App-managed caches, project-scope plugins, local/custom skills, GUI casks, untrusted taps, OS/Xcode/Simulator updates, and prohibited graph work remained untouched.

## 2026-08-22T11:03:34+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or uncommitted project work was entered or changed, and no MCP server started.
- Claude Code had advanced through its healthy native auto-update before the run `2.1.238 -> 2.1.239`. Refreshed all eight configured Git marketplaces and seven user-scope marketplace plugins while preserving scope/enabled state; `diagram-design` updated `2.6.1 -> 2.6.5`.
- Matt advanced `0ab1b63a410a -> 5b15a47f2d71`. Its manifest and 24-skill managed set did not change: 23 managed copies match new upstream and the existing `code-review` local delta remains preserved. The only semantic delta added the un-managed in-progress `implement-spec` skill with explicit branch/PR/merge/worktree external-write behavior; it was recorded but not copied into managed skills or local policy. Manifest/runtime and real-directory validation passed.
- Refreshed Homebrew metadata `ac9e7acf4e -> af3d8ec186`, then upgraded trusted vendor CLI Sentry `0.42.2 -> 0.43.0` from the exact dry-run. Linkage/runtime checks and `brew missing` passed; no formula remains outdated and untrusted-tap protection stayed enabled.
- Updated `code-review-graph` `2.3.7 -> 2.3.8` using only `uv tool upgrade code-review-graph`; Voice Mode stayed `8.12.0` while isolated SciPy updated `1.18.0 -> 1.18.1`, and Pyright stayed `1.1.411`. CRG version validation passed, and no installer, graph, schema, server, daemon, embed, hook, or Graphify command ran. `uv tool upgrade --all --dry-run` is unsupported, so PyPI metadata plus exact per-tool upgrades supplied the bounded target checks.
- Config-only MCP auditing resolved floating Context7 `4.0.3`, Playwright `0.0.79`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current and disabled fixed Context7 `4.0.0` remains held. The prior llm-wiki entry is absent from current top-level user MCP configs; no config was changed. Notion/Slack reauthorization remains explicitly skipped, and no MCP list/health/server command ran.
- Codex standalone `0.149.0` matches official GitHub/npm metadata, so no updater ran; configured Git marketplaces are current. Doctor passed with 0 failures; 2,338 rollout files missing state-DB rows remain an existing cleanup warning and no cleanup ran.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.22.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.51.0`, mise `2026.8.10`, uv `0.12.5`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current. npm/pnpm/Bun/Cargo global name sets, credential presence/source types, runtime paths, and installed mise runtimes are unchanged and verified without exposing secrets.
- Retry only on changed evidence or explicit direction: `matt-code-review-local-delta`, `context7-fixed-pin`, Vercel major `58.9.4 -> 59.3.0`, Block Buzz GUI `0.5.11 -> 0.5.18`, gcloud CLI cask major `553.0.0 -> 581.0.0`, Orca GUI `1.4.168 -> 1.4.185`, Notion/Slack OAuth, and Codex rollout-state cleanup. App-managed caches, project-scope plugins, local/custom skills, GUI casks, untrusted taps, OS/Xcode/Simulator updates, and prohibited graph work remained untouched.

## 2026-08-23T10:28:34+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or uncommitted project work was entered or changed, and no MCP server started.
- Claude Code had advanced through healthy native auto-update before the run `2.1.239 -> 2.1.241`; the official updater confirmed current and Doctor passed. Refreshed all eight configured Git marketplaces and checked all seven user-scope marketplace plugins with official commands while preserving scope/enabled state; all remain current.
- Matt took the fast path at unchanged HEAD `5b15a47f2d71` and unchanged manifest. Manifest/runtime inventory is current at 24 managed skills, all 25 upstream skill directories contain `SKILL.md`, marketplace validation passed, and the existing `code-review` plus newly recorded `grilling` local deltas remain preserved; 22 other managed copies match upstream.
- Refreshed Homebrew metadata `af3d8ec186 -> aad366ffaf`, then upgraded Mole `1.51.0 -> 1.52.0` and OpenCC `1.4.1 -> 1.4.2` from the exact dry-run. Linkage/runtime checks and `brew missing` passed; only held `usage 5.1.0 -> 6.1.0` remains formula-outdated, and untrusted-tap protection stayed enabled.
- `code-review-graph` stayed `2.3.8` while isolated cyclopts updated `4.23.1 -> 4.23.2`; Voice Mode stayed `8.12.0` with the same isolated dependency update, and Pyright stayed `1.1.411`. CRG used only `uv tool upgrade code-review-graph` and `code-review-graph --version`; no installer, graph, schema, server, daemon, embed, hook, or Graphify command ran.
- Config-only MCP auditing resolved floating Context7 `4.0.3`, Playwright `0.0.79`, and Chrome DevTools MCP `1.7.0`; fixed GCP `0.5.3` is current and disabled fixed Context7 `4.0.0` remains held. Remote/OAuth MCP state was not probed, Notion/Slack authorization remained skipped, and no MCP list/health/server command ran.
- Codex standalone `0.149.0` matches official npm metadata, so the known unsupported updater was not retried; configured Git marketplaces are current. Doctor passed with 0 failures; 417 stale rollout rows remain an existing cleanup warning and no cleanup ran.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.22.0`, Bun `1.4.0`, gh `2.98.0`, mise `2026.8.10`, uv `0.12.5`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current. Global package-name sets and credential presence/source types are unchanged. Rust binaries passed through `~/.cargo/bin`, but this Orca login PATH did not resolve their bare command names; no PATH setting changed.
- Retry only on changed evidence or explicit direction: `matt-code-review-local-delta`, `matt-grilling-local-delta`, `context7-fixed-pin`, Vercel major `58.9.4 -> 59.5.0` (pnpm currently resolves `59.3.0`), `usage-major-6`, Block Buzz GUI `0.5.11 -> 0.5.18`, gcloud CLI cask `553.0.0 -> 581.0.0`, Orca GUI, Notion/Slack OAuth, `rust-path-resolution`, and Codex rollout-state cleanup. App-managed caches, project-scope plugins, local/custom skills, GUI casks, untrusted taps, OS/Xcode/Simulator updates, and prohibited graph work remained untouched.

## 2026-08-23T10:44:20+0800 approved maintenance follow-up

- User approved every previously manual maintenance item except Notion/Slack authorization. Completed the follow-up from `$HOME` without entering a product repo or Orca workspace and without changing schedulers, project state, MCP authorization, graph data, or secrets.
- Upgraded Homebrew formula `usage 5.1.0 -> 6.1.0`, pnpm-global Vercel `58.9.4 -> 59.3.0`, Block Buzz cask `0.5.11 -> 0.5.18`, and gcloud CLI cask `553.0.0 -> 581.0.0`. Vercel used pnpm's normal `latest` resolution; gcloud rebuilt only its cask-managed component set and isolated virtualenv without login.
- Added the existing `~/.cargo/env` loader to `~/.zprofile`; fresh login-shell checks now resolve rustup, rustc, and cargo through `~/.cargo/bin`. The Matt `code-review` and `grilling` deltas are accepted as intentional local policy and remain preserved; their manual retry keys are closed.
- Orca cask was not changed because Homebrew's only offered transaction is a deprecated downgrade from registered `1.4.168` to `1.3.1`, while the running Orca reports `1.4.188`. Retry only when Homebrew offers a non-deprecated version newer than the installed app.
- Codex rollout-state cleanup was authorized but remains blocked safely: OpenAI Docs and Codex `0.149.0` expose diagnosis plus legacy rollout migration, but no supported stale-row repair command. Doctor still reports healthy databases, 417 stale rows, and 0 failures; no direct SQLite mutation or file deletion was attempted. Retry when Codex provides a supported repair interface.
- Validation passed: `usage --version`, Homebrew linkage and `brew missing`, `vercel --version`, empty pnpm outdated output, gcloud version, Block Buzz cask registration, fresh login-shell Rust paths, and final Codex Doctor. Only Orca remains Homebrew-outdated. Notion/Slack OAuth remains explicitly skipped.

## 2026-08-23T11:27:24+0800 sustainable setup optimization

- Completed the authorized projectless setup convergence from `$HOME`; no product repo, Orca workspace, scheduler, project dependency, graph data, or OAuth state was read or changed, and no secret was exposed or changed. OpenAI Docs confirms the existing Codex standalone install/update path is the supported macOS/Linux route, so Codex ownership stayed unchanged.
- Replaced enabled Claude MCP runtime floats with reviewed exact pins: Context7 `4.0.3` and Playwright `0.0.79`; added non-interactive Playwright resolution. Modernized disabled Codex Context7/GCP configs from absolute Node `24.15.0` paths to `npx`, updated disabled Context7 to `4.0.3`, and kept both disabled. JSON/TOML parsing passed and no MCP server started.
- Converged shell startup: removed all global Linear/Zeabur credential exports, kept Zeabur as an explicit process-scoped `with-zeabur-token` wrapper, and removed dead Symphony shortcuts after confirming no binary, package, credential, or other user-level consumer exists. Reduced PATH composition to one ordered block in `~/.zshrc` plus the installer block in `~/.zprofile`; fresh clean login shells resolve all managed tools with 39 unique PATH entries and no inherited maintenance credential.
- Confirmed NVM default Node `24.19.0` is the sole active user-level Node owner; mise's installed Node `24.15.0` is inactive and retained without mutation for project compatibility. No NVM version was removed because product `.nvmrc` files were outside this projectless audit.
- With explicit source-trust approval, trusted only exact artifacts and upgraded OpenCode `1.15.7 -> 1.18.21`, Archon `0.3.12 -> 0.9.0`, Heroku CLI `11.4.0 -> 11.10.0`, FVM `4.1.0 -> 4.1.5`, and CodexBar `0.27.0 -> 0.54.0`; OpenCode added trusted Homebrew-core ripgrep `15.2.0`. Removed the unused `shopify/shopify` tap; no whole third-party tap was trusted.
- Validation passed: all five updated runtimes, four formula linkage checks, `brew missing`, Homebrew trust inventory, `brew doctor`, shell syntax and clean-login resolution, MCP config parsing, Claude Doctor, and Codex Doctor. No formula or cask is outdated except Orca; Codex reported 0 failures.
- Remaining retry keys are evidence-gated only: `orca-cask-owner` when the tap offers a non-deprecated version newer than the running app, `codex-rollout-repair` when Codex exposes a supported stale-row repair interface, and exact MCP pin updates when registry versions change with reviewed compatibility. Notion/Slack OAuth remains explicitly skipped.

## 2026-08-23T20:38:00+0800 user-level skill and agent setup optimization

- Preserved the user's full-trust agent model: no filesystem permission, sandbox, approval, or shell-environment visibility restriction changed. No credential was displayed, rotated, or rewritten.
- Converged Vercel React guidance to one canonical `~/.agents/skills/vercel-react-best-practices` directory whose folder matches its skill name; Claude now uses a thin symlink. The obsolete `design-taste-frontend-v1` entry was already absent at execution time.
- Refactored `brandkit`, `design-taste-frontend`, `image-to-code`, `imagegen-frontend-mobile`, and `imagegen-frontend-web` from 798–1,465-line entrypoints to 32–36-line routers plus 26 branch-specific references. Exact range checks proved every moved rule remains present; all local links resolve.
- Moved 31 exact derived targets to macOS Trash, reducing active setup trees by about 4.72 MB: one duplicate Claude skill copy, test caches, superseded Codex backup/tmp files, old Doctor quarantine, and three Claude recovery backups. Six review documents under `~/.claude/plans` were retained because they contain product decisions, release gates, and regression evidence rather than disposable plans.
- Validation passed for all six touched skills with the official skill validator, global skill discovery resolves 82 skills including the renamed Vercel skill, Git whitespace checks pass, Claude Doctor reports native latest-channel `2.1.241` healthy, and Codex Doctor `0.149.0` reports 0 failures. The full Claude agent-contract suite is 12 pass / 4 fail on pre-existing Graph contract drift; no Graph file was changed.
- Remaining boundaries: setup edits stay uncommitted because `~/.agents` is on a non-default branch and `~/.claude` contains unrelated dirty work. Their Git/history convergence requires an explicit user-only `$git-converge-main` invocation. Codex stale rollout repair remains evidence-gated on a supported repair interface; Notion/Slack OAuth remains skipped.

## 2026-08-23T20:48:44+0800 historical setup-backup cleanup

- Permanently removed 50 exact user-level setup targets totaling 6,517,690 bytes: 19 active backup/tmp paths for Claude, shell, and OpenCode plus the 31 previously reviewed setup artifacts in macOS Trash. The Trash itself was not emptied.
- Kept all 20 `~/.claude/backups/pre_compact_*.jsonl` records because they are conversation-recovery history, not setup backups. Kept product review/gate documents and legitimate recovery/backup reference documentation.
- Post-cleanup inventory reports zero matching active setup-backup candidates, zero targeted Trash remainders, and no non-`pre_compact` children under `~/.claude/backups`.
- Validation passed for current Claude/OpenCode JSON, `~/.zshrc` syntax, Claude Code `2.1.241`, Codex CLI `0.149.0`, OpenCode `1.18.21`, and global skill discovery through `npx skills`. A persistent `skills` binary is unnecessary because the disposable `npx skills` entrypoint works.
- Preserved the full-trust permission model. No product repo, Orca workspace, scheduler, Notion/Slack state, credential, or graph data was read or changed, and no normal runtime data was deleted.

## 2026-08-23T21:51:13+0800 setup-hygiene workflow creation

- Created the explicit-only canonical `~/.agents/skills/setup-hygiene` skill with one verdict reference, a deterministic read-only scan, a relative Claude skill symlink, and a thin `~/.claude/automations/setup-hygiene/PROMPT.md` job contract. No scheduler was registered or changed, and first-run state was deliberately not created before a real invocation.
- Two independent latest-Opus Claude Code consults shaped and forward-tested the workflow. The second found six coverage defects; all were corrected: Claude-only skill enumeration, MCP/plugin referential inventory, plugin-data ownership, Claude recovery-tree ownership, first-run state authority, and effective-PATH coverage.
- Live read-only scan reconciles 33 records: 73 canonical skills; all 43 Claude skill entries accounted for as 35 symlinks, two duplicate real owners, and six Claude-only real owners; 14 MCP servers, five enabled and ten installed plugins, four configured and eight known marketplaces, six skill overrides, and 19 local config pointers with zero unresolved references. Three historical root-level setup backups remain review-only; runtime recovery trees are telemetry, not cleanup candidates.
- Validation passed: in-memory Python compile, YAML/invocation-policy contract, global `npx skills` discovery, local links, relative Claude symlink, protected-file hashes and setup-repo status digests unchanged across the live scan, zero absolute-home leakage, and an isolated forward-test for quoted YAML, fenced examples, broken live pointers, `.bak-pre-*`, contextual app commands, and symlink repair before/after behavior. The official `quick_validate.py` could not run because the system-managed `skill-creator` directory became unavailable during the session; it was not reconstructed.
- The workflow does not duplicate daily updater responsibilities: it owns structural integrity, pointer coherence, sediment evidence, and bounded state; the updater retains versions, installs, marketplaces, plugins, MCP registry currency, and runtime health. Permission policy, secrets, OAuth, product repos, Orca workspaces, graph data, Git history, recovery records, and Trash stayed unchanged.

## 2026-08-24T09:45:43+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or uncommitted project work was entered or changed, and no MCP server started.
- Refreshed Homebrew metadata `96002be435 -> 0942cac2ed`, then upgraded FVM `4.1.5 -> 4.2.0`, usage `6.1.0 -> 6.1.1`, and mise `2026.8.10 -> 2026.8.11` from the exact dry-run. Corepack upgraded pnpm `11.22.0 -> 11.23.0`; the pnpm-global package-name set stayed unchanged. Formula linkage, direct runtime checks, and `brew missing` passed.
- Claude Code stayed current at `2.1.241`; the official updater reported current and Doctor found no installation issue, although its last-update telemetry records one `install_failed` attempt. Refreshed all eight configured marketplaces and checked all seven user-scope marketplace plugins with official commands while preserving versions, scopes, and enabled states. Matt took the fast path at unchanged HEAD `5b15a47f2d71` and manifest `1.2.3`; reconcile/runtime inventory passed at 24 skills, source validation passed, and the accepted `code-review`/`grilling` local policy deltas remained preserved.
- Codex's official latest changed `0.149.0 -> 0.149.1`, but the official updater again could not detect the Orca-managed standalone installation method, so Codex remains `0.149.0`; no App-managed path or alternate installer was mutated. Both configured Codex Git marketplaces are current. Doctor reports 0 failures, healthy databases, and the unchanged 417 stale rollout rows; retry keys are `codex-standalone-update` when the App owner or updater supports this installation and `codex-rollout-repair` when a supported repair interface exists.
- Config-only MCP registry auditing confirmed fixed Context7 `4.0.3`, Playwright `0.0.79`, and GCP `0.5.3` are current, and floating Chrome DevTools MCP resolves to `1.7.0`. Remote/App-managed entries were not health-probed; Notion/Slack OAuth remains explicitly skipped. `code-review-graph` stayed `2.3.8` through only `uv tool upgrade code-review-graph` and `code-review-graph --version`; no installer, graph, schema, server, daemon, embed, hook, or Graphify command ran.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.52.0`, uv `0.12.5`, Pyright `1.1.411`, Voice Mode `8.12.0`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current. Actual binary paths and global package-name sets were rechecked; Rust passed through `~/.cargo/bin`.
- Deferred CodexBar GUI `0.54.0 -> 0.54.1` pending an explicit GUI-cask decision. Vercel remains `59.3.0` because pnpm's existing publication-age policy still selects it while registry latest is `59.5.0`. The Orca tap remains deprecated at `1.3.1`, below the running app `1.4.188`; retry `orca-cask-owner` only when the tap offers a non-deprecated newer version. App-managed caches, project-scope plugins, local/custom skills, other GUI/OS/Xcode/Simulator work, untrusted taps, and prohibited graph work remained untouched.

## 2026-08-24T09:53:49+0800 Codex standalone update follow-up

- Confirmed from OpenAI's official CLI documentation and inspected installer that the standalone installer supports a caller-selected `CODEX_HOME`, uses the existing release-cache layout, verifies the package checksum, acquires the install lock, and atomically switches the `current` symlink. The live installer digest matched the official `0.149.1` release asset.
- Updated the Orca-owned standalone Codex `0.149.0 -> 0.149.1` with the official installer, targeting the owner home derived from the resolved `~/.local/bin/codex` path and keeping `CODEX_INSTALL_DIR=~/.local/bin`. The exact release and non-interactive mode were used; no npm/Homebrew duplicate, auth/config change, scheduler change, or project/repository access occurred.
- Verified `codex --version`, both visible command symlinks, package manifest `layoutVersion=1`, target `aarch64-apple-darwin`, and both retained release binaries. `0.149.0` remains available for rollback while `current` resolves to `0.149.1`. Codex Doctor reports 21 checks OK and 0 failures; both configured Git marketplaces remain current. The existing 417 stale rollout rows were not changed.
- The `codex-standalone-update` retry key is resolved. For future version changes in this Orca-owned layout, validate the resolved command is under the expected Orca account `home/packages/standalone` root, then use the official installer with that owner home as `CODEX_HOME`; the generic `codex update` command does not recognize this ownership layout.

## 2026-08-25T09:30:01+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or project work was entered or changed, and no MCP server started.
- Refreshed Homebrew metadata `0942cac2ed -> b56087c951`, then upgraded FFmpeg `9.0.1 -> 9.0.1_1`, FVM `4.2.0 -> 4.3.0`, libksba `1.8.0 -> 1.8.1`, little-cms2 `2.19 -> 2.19.1`, mise `2026.8.11 -> 2026.8.12`, OpenCode `1.18.21 -> 1.18.22`, usage `6.1.1 -> 6.4.0`, and x265 `4.2 -> 4.3`. Updated global pi-coding-agent `0.84.2 -> 0.84.3`; the global package-name set and existing install-script allowlist stayed unchanged. All linkage/runtime checks, `brew missing`, and Homebrew Doctor passed.
- Claude Code stayed current and healthy at native `2.1.243`. Refreshed all eight configured marketplaces and checked all seven user-scope marketplace plugins while preserving versions, scopes, and enabled states. Addy advanced `95d6e25 -> afa8da9`, Marketing Skills `f557c54 -> becd60e`, and Matt `5b15a47 -> 6654f6b`; Matt's only delta added an excluded in-progress `retro` stub, so the 24-skill desired set stayed unchanged. Reconcile/runtime and plugin validation passed, and the accepted `code-review`/`grilling` local policy deltas remained preserved.
- Config-only MCP registry auditing confirmed fixed Context7 `4.0.3`, Playwright `0.0.79`, and GCP `0.5.3` are current, while floating Chrome DevTools MCP resolves to `1.7.0`. Remote/App-managed entries were not health-probed; Notion/Slack OAuth remains explicitly skipped. CRG stayed `2.3.8` while isolated `mcp 1.29.0 -> 1.29.1` and `platformdirs 4.11.3 -> 4.11.4` refreshed; Voice Mode `8.12.0` received the same dependency refresh. No installer, graph, schema, server, daemon, embed, hook, or Graphify command ran.
- Codex standalone `0.149.1` matches official latest and its configured Git marketplace is current. Doctor reports 0 failures and healthy databases; the former 417 stale rows are now 0, while 2,644 rollout files are missing active state-DB rows. Retry `codex-rollout-repair` only when a supported repair interface exists; no SQLite or rollout-file mutation was attempted.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.23.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.52.0`, uv `0.12.5`, Pyright `1.1.411`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current. Deferred GUI casks CodexBar `0.54.0 -> 0.55.0` and McBopomofo `3.0 -> 3.1`; Vercel remains policy-selected at `59.3.0` while registry latest is `59.5.0`. Retry only on explicit GUI approval, changed publication-age selection, or changed supported repair evidence.

## 2026-08-26T09:21:03+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or project work was entered or changed, and no MCP server started.
- Refreshed Homebrew metadata `b56087c951 -> 9574410112`, then upgraded libvpx `1.16.0 -> 1.17.0`, mise `2026.8.12 -> 2026.8.13`, and OpenCode `1.18.22 -> 1.18.23` from the exact dry run. Updated Corepack-managed pnpm `11.23.0 -> 11.24.0` and standalone uv `0.12.5 -> 0.12.6`; Voice Mode stayed `8.12.0` while its isolated uv dependency followed to `0.12.6`. Formula linkage, libvpx dylib inspection, direct runtimes, `brew missing`, and Homebrew Doctor passed; global package-name sets stayed unchanged.
- Claude Code had already advanced through healthy native auto-update `2.1.243 -> 2.1.246`. Refreshed all eight configured marketplaces and all seven user-scope marketplace plugins while preserving scope/enabled state; disabled `code-review` advanced `eb1badf08049 -> b819188d2eea`. Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; reconcile/runtime inventory and source/plugin validation passed for 24 skills, and the accepted `code-review`/`grilling` local policy deltas remained preserved.
- Config-only MCP registry auditing confirmed fixed Context7 `4.0.3`, Playwright `0.0.79`, and GCP `0.5.3` are current; floating Chrome DevTools MCP now resolves `1.7.0 -> 1.8.0`. Remote/App-managed entries were not health-probed and Notion/Slack OAuth remains explicitly skipped. CRG stayed `2.3.8` through its permitted upgrade/version path; Graphify remains absent and no installer, graph, schema, server, daemon, embed, hook, or repo command ran.
- Codex standalone `0.149.1` matches official metadata, and its configured Git marketplace is current. Doctor passed with 0 failures and healthy databases; current evidence again reports 417 stale rollout rows with no missing active or archived rows. No state cleanup or direct database/file mutation ran; retry `codex-rollout-repair` only when Codex exposes a supported repair interface.
- Node/default `24.19.0`, npm `12.0.2`, Corepack `0.35.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.52.0`, Pyright `1.1.411`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current. Mise self-reports upstream `2026.8.14`, but its Homebrew owner still offers `2026.8.13`; retry only when `brew info mise` changes. Deferred GUI casks CodexBar `0.54.0 -> 0.55.0` and McBopomofo `3.0 -> 3.1`; Vercel remains `59.3.0` while pnpm reports latest `59.5.0` but wanted `59.3.0`. Retry GUI items only on explicit approval and Vercel only when `wanted` changes.

## 2026-08-27T10:11:59+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or project work was entered or changed, and no MCP server started.
- Refreshed Homebrew metadata `9574410112 -> e6a9053e7a`, then upgraded mise `2026.8.13 -> 2026.8.14`, usage `6.4.0 -> 6.4.1`, and official-tap Supabase CLI `2.115.0 -> 2.116.0`. Upgraded NVM default Node `24.19.0 -> 24.20.0`, preserved npm `12.0.2`, Corepack `0.35.0`, and the four-name global package set, and retained Node `24.19.0` for rollback. All formula linkage, runtimes, `brew missing`, and Homebrew Doctor passed; blocked npm install scripts remained untrusted.
- Claude Code had already advanced through healthy native auto-update `2.1.246 -> 2.1.247`. Refreshed all eight configured marketplaces and all seven user-scope marketplace plugins while preserving scope/enabled state; only marketingskills advanced `becd60ee9df0 -> b1aaa3619e74`. Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; the 24-skill Claude runtime and source validation passed, and accepted `code-review`/`grilling` local policy deltas remain preserved. The cross-client managed subset is missing the old-managed `~/.agents/skills/prototype` copy; fast-path rules prohibited per-file repair. Retry `matt-prototype-missing` on an upstream Matt delta or explicit repair direction.
- Updated Codex standalone `0.149.1 -> 0.150.1` with the official installer and exact Orca-owned `CODEX_HOME`; package layout/target, visible path, retained rollback releases, and the configured Git marketplace passed. Codex Doctor reports 0 failures and healthy databases; the missing rollout/state-row warning persists. Retry `codex-rollout-repair` only when Codex exposes a supported repair interface; no database or rollout-file mutation ran.
- Config-only MCP registry auditing confirmed fixed Context7 `4.0.3`, Playwright `0.0.79`, and GCP `0.5.3` are current, and floating Chrome DevTools MCP resolves to `1.8.0`. CRG stayed `2.3.8` while isolated click `8.4.2 -> 8.5.0`, cyclopts `4.23.2 -> 4.23.3`, and websockets `17.0.1 -> 17.1` refreshed; Voice Mode `8.12.0` received the same dependency refresh plus OpenAI `3.3.1 -> 3.5.0`. Graphify remains absent; no installer, graph, schema, server, daemon, embed, hook, or repo command ran.
- npm `12.0.2`, Corepack `0.35.0`, pnpm `11.24.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.52.0`, uv `0.12.6`, Pyright `1.1.411`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current. Vercel stays `59.3.0` because pnpm still reports wanted `59.3.0`; retry only when wanted changes. Deferred GUI casks are Block Buzz `0.5.18 -> 0.5.20`, CodexBar `0.54.0 -> 0.55.1`, and McBopomofo `3.0 -> 3.1`; the deprecated Orca cask ownership mismatch is unchanged. Notion/Slack OAuth remains explicitly skipped.

## 2026-08-28T09:36:07+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or project work was entered or changed, and no MCP server started.
- Refreshed Homebrew metadata/runtime `e6a9053e7a -> 912f7f9f12`, then upgraded HarfBuzz `14.3.1 -> 14.4.0`, libgcrypt `1.12.2 -> 1.12.3`, and Sentry CLI `0.43.0 -> 0.44.0` from the exact dry run. Upgraded native Claude Code `2.1.247 -> 2.1.250` and standalone uv `0.12.6 -> 0.12.7`. Formula linkage, direct runtimes, `brew missing`, Homebrew Doctor, Claude Doctor, and uv runtime checks passed.
- Refreshed all eight Claude marketplaces and all seven user-scope marketplace plugins while preserving scope and enabled state. Disabled `code-review` advanced `b819188d2eea -> e33a9ec0973a`, and enabled `diagram-design` advanced `2.6.6 -> 2.6.7`; project-scope and skills-dir installations were not updated through the marketplace-plugin path. Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; all 24 managed copies, including `prototype`, are present, reconcile/runtime and source/plugin validation passed, and the accepted `code-review`/`grilling` local policy deltas remain preserved. The former `matt-prototype-missing` retry key is resolved.
- Config-only MCP registry auditing confirmed fixed Context7 `4.0.3`, Playwright `0.0.79`, and GCP `0.5.3` are current, while floating Chrome DevTools MCP resolves to `1.8.0`. Remote/App-managed entries were not health-probed, and the explicit Notion/Slack no-reauthorization decision remains in effect. `code-review-graph` stayed `2.3.8` while isolated `platformdirs 4.11.4 -> 4.11.5` refreshed through its permitted upgrade/version path; Graphify remains absent and no installer, graph, schema, server, daemon, embed, hook, or repo command ran.
- Codex standalone `0.150.1` matches official metadata; its configured Git marketplace is current, and runtime/App-managed caches stayed untouched. Codex Doctor reports 0 failures and healthy databases, with `2,791` active rollout files missing state-DB rows and no stale rows. Retry `codex-rollout-repair` only when Codex exposes a supported repair interface; no database or rollout-file mutation ran.
- Node/default `24.20.0`, npm `12.0.2`, Corepack `0.35.0`, pnpm `11.24.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.52.0`, mise `2026.8.14`, Pyright `1.1.411`, Voice Mode `8.12.0`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current. npm, pnpm, and Bun global package-name sets are unchanged. Vercel remains policy-selected at `59.3.0`; retry only when pnpm `wanted` changes. Deferred GUI casks remain Block Buzz `0.5.18 -> 0.5.20`, CodexBar `0.54.0 -> 0.55.1`, and McBopomofo `3.0 -> 3.1`; the deprecated Orca cask ownership mismatch is unchanged.

## 2026-08-29T11:06:04+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or project work was entered or changed, and no MCP server started.
- Refreshed Homebrew metadata/runtime `912f7f9f12 -> 4710c526b3`, then upgraded NSS `3.127 -> 3.128`, OpenCode `1.18.23 -> 1.18.25`, and usage `6.4.1 -> 6.5.0` from the exact dry run. All formula linkage checks, direct runtime/library gates, `brew missing`, and Homebrew Doctor passed; no formula remains outdated.
- Upgraded native Claude Code `2.1.250 -> 2.1.251`. Refreshed all eight configured marketplaces and all seven user-scope marketplace plugins while preserving scope and enabled state; marketingskills advanced `b1aaa3619e74 -> e55de886fe75`, and disabled `code-review` advanced `0620a687ddd5 -> ed404106fcd8`. Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; reconcile/runtime passed for 24 skills, source/plugin validation passed, and accepted `code-review`/`grilling` local policy deltas remain preserved.
- Codex standalone `0.150.1` matches official registry and cached update metadata; its diagram-design Git marketplace is current, and runtime/App-managed caches stayed untouched. Codex Doctor found 0 failures and healthy databases but again reports 417 stale rollout rows with no missing rows. Retry `codex-rollout-repair` only when Codex exposes a supported repair interface; no database or rollout-file mutation ran.
- Config-only MCP auditing confirmed fixed Playwright `0.0.79` and GCP `0.5.3` plus floating Chrome DevTools MCP `1.8.0` are current. Fixed Context7 `4.0.3` remains held while registry latest is `4.0.4`; retry `context7-fixed-pin` only with reviewed compatibility or migration evidence. `code-review-graph` stayed `2.3.8` while isolated Pydantic `2.13.4 -> 2.13.5` and pydantic-core `2.46.4 -> 2.46.5` refreshed through its sole permitted upgrade/version path. Graphify remains absent and no prohibited graph operation ran.
- Updated npm globals pi-coding-agent `0.84.3 -> 0.84.4` and Corepack `0.35.0 -> 0.36.0`; the four-name npm global set, one-name pnpm global set, and blocked install-script policy stayed unchanged. Voice Mode stayed `8.12.0` while isolated OpenAI `3.5.0 -> 3.6.0`, platformdirs `4.11.4 -> 4.11.5`, Pydantic, pydantic-core, and uv `0.12.6 -> 0.12.7` refreshed. Node/default `24.20.0`, npm `12.0.2`, pnpm `11.24.0`, Bun `1.4.0`, uv `0.12.7`, Pyright `1.1.411`, gh `2.98.0`, Mole `1.52.0`, mise `2026.8.14`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current; npm Doctor passed.
- Vercel remains policy-selected at `59.3.0` because pnpm `wanted` is unchanged despite registry latest `59.9.1`. Deferred GUI casks are Block Buzz `0.5.18 -> 0.5.20`, CodexBar `0.54.0 -> 0.56.0`, and McBopomofo `3.0 -> 3.1`; the Orca receipt/running-app/tap ownership mismatch also remains deferred. Notion/Slack OAuth remains explicitly skipped.

## 2026-08-30T09:27:29+0800 daily development-tool update

- Completed the unattended update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or project work was changed, and no MCP server started. One over-broad read-only installer-candidate search traversed Codex session JSONL files and returned only matching file paths; it exposed no session content and changed nothing, but exceeded the intended tool-and-memory read boundary.
- Refreshed Homebrew metadata `4710c526b3 -> b42173be7f` and upgraded official-tap Sentry CLI `0.44.0 -> 0.44.1` from the exact dry run. Formula linkage, direct runtime, `brew missing`, and Homebrew Doctor passed; no formula remains outdated.
- Updated the Orca-owned standalone Codex `0.150.1 -> 0.151.0` with the official checksum-verifying installer and exact owner `CODEX_HOME`. Visible symlinks, package manifest `layoutVersion=1`, `aarch64-apple-darwin` target, retained rollback releases, registry/cached latest metadata, and Git marketplace passed. Doctor found no installation, auth, database-integrity, or update failure, but reports `3,388` active rollout files missing state rows, one ignored legacy-config warning, and 4.8 GiB free disk; retry `codex-rollout-repair` only with a supported repair interface, and disk/config cleanup only with explicit scope or a material threshold change.
- Refreshed all eight Claude marketplaces and all seven user-scope marketplace plugins while preserving scope and enabled state. Diagram Design advanced `2.6.7 -> 2.6.11` and its Git HEAD `ac490fd1ac4b -> b52a33bfeef8`; plugin validation passed. Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; reconcile/runtime and source/plugin validation passed for 24 skills, with accepted `code-review` and `grilling` local deltas preserved. Claude Code remains current at native `2.1.251`; Doctor finds no installation issue, although its latest auto-update attempt is recorded as `install_failed`, so retry only on a version gap or repeated failure evidence.
- Config-only MCP auditing confirmed fixed Playwright `0.0.79` and GCP `0.5.3` plus floating Chrome DevTools MCP `1.8.0` are current. Fixed Context7 `4.0.3` remains held while registry latest is `4.0.4`; Notion/Slack and remote/App-managed auth remain skipped. `code-review-graph` stayed `2.3.8` while its isolated `joserfc 1.7.4 -> 1.7.5` refreshed through the sole permitted upgrade/version path; Voice Mode `8.12.0` received the same dependency refresh. Graphify remains absent and no prohibited graph operation ran.
- Node/default `24.20.0`, npm `12.0.2`, Corepack `0.36.0`, pnpm `11.24.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.52.0`, mise `2026.8.14`, uv `0.12.7`, Pyright `1.1.411`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current; runtime paths and global package-name sets are unchanged. Credential checks reported Claude `claude.ai`, Codex ChatGPT file storage, and gh keyring sources without exposing values.
- Vercel remains policy-selected at `59.3.0` because pnpm `wanted` is unchanged while registry latest is `59.10.0`. Deferred GUI casks are Block Buzz `0.5.18 -> 0.5.20`, CodexBar `0.54.0 -> 0.56.0`, and McBopomofo `3.0 -> 3.1`; the deprecated Orca cask receipt/tap/running-app ownership mismatch remains unchanged. Retry only on changed policy evidence, explicit GUI approval, or a supported ownership repair.

## 2026-08-30T09:48:27+0800 approved GUI and disk-capacity follow-up

- Upgraded Homebrew cask CodexBar `0.54.0 -> 0.56.0` after explicit approval. Its CLI reports `0.56.0`, and strict code-signature plus Gatekeeper assessment passed. McBopomofo remains `3.0` because `3.1` is a manual-installer artifact and the active input-method process would need interruption; no unsupervised installer or process stop ran. Block Buzz remains deferred.
- Recovered exact rebuildable caches without broad cleanup: `uv cache clean` removed 80,241 files and 2.4 GiB, npm cache was cleared, and pnpm pruned metadata but no package bytes. Homebrew cleanup was deliberately skipped because its dry run would autoremove installed `usage`. Free space rose from about 1.2 GiB to 3.3 GiB, and then to 4.9 GiB after CodexBar's cask purge.
- Mole `clean --dry-run` found 154 candidates / 5.47 GiB but includes mixed-risk Trash, Xcode, Homebrew, and app data, so no broad Mole deletion ran. Short disk-write loops did not reproduce a rapid active drain. Instead, all 788,128 KiB of the current uv cache, spanning 30,327 recent files, rebuilt during this maintenance and no uv writer remained; free space consequently returned to about 3.1 GiB.
- Capacity pressure is cumulative: encrypted swap uses about 9.0 GiB after long uptime, one simulator remains booted, Xcode DerivedData is about 13.0 GiB, simulator device data about 8.6 GiB plus mounted runtimes, and Docker data about 14.5 GiB. Trash is about 1.1 GiB and deleted-open files about 1.1 GiB. Mole history repeatedly shows 4.7-6.3 GiB of recoverable churn. The evidence supports low headroom plus routine cache/swap/regeneration, not a currently active single-process leak. A read-only Docker inventory query hung and was interrupted without mutation.
- No product repo or Orca system-automations workspace was entered or changed, and no scheduler, OAuth, credential, graph, project dependency, or product data was changed.

## 2026-08-30T09:56:14+0800 Docker capacity diagnosis follow-up

- Docker is not a current high-rate writer. Its sparse `Docker.raw` has a 494,384,709,632-byte logical ceiling but 14,812,896 KiB physically allocated, had not changed since `2026-08-29T19:57:49+0800`, remained unchanged across a 10-second red-capable growth check, and was not open by any process. No Docker VM/virtualization process is running; the local engine endpoint reports that Docker Desktop is unable to start.
- The direct failure is host ENOSPC, not an active container storm: Docker logged 12 `no space left on device` signals and stopped its Linux VM at the same timestamp as the raw image's last modification. Docker logs occupy only 72,392 KiB. The evidence establishes that a full host stopped Docker, but cannot establish that Docker originally filled the host.
- The final 73 minutes of available VM API logs show one local Supabase test stack, not unbounded duplicates: 7 container creates, 7 starts, 5 deletes, 4 stop/kill/restart operations, 3 volume creates, one volume delete, 10 concurrently polled containers, and 19 shim disconnects across 11 container IDs. Eight image pulls included three rapid retries each for the same storage-api and gotrue tags; multiple pulls ended with errors before registry fallback. Content may include incomplete/reclaimable pull data, but exact image, build-cache, and volume bytes cannot be enumerated until the daemon can start.
- No Docker start, restart, prune, deletion, reset, mount, or product-repository access ran. After host headroom is restored, the safe next diagnostic is a live `docker system df -v` plus container/image/volume grouping before any cleanup decision.

## 2026-08-30T10:03:52+0800 authorized Docker restart and live inventory

- The normal Docker Desktop restart stalled on the old crashed backend. After explicit daemon-restart authorization, forced only the stale Docker Desktop processes to stop and started Docker Desktop cleanly; no data reset or deletion ran. Engine `_ping` is now `OK`, and all nine containers in the single Supabase local-test stack are healthy. The forced stop released host resources and temporarily raised free space from about 4.1 GiB to 6.6 GiB.
- Live inventory disproves duplicate running stacks: Docker has 9 running / 0 stopped containers with only 29.14 MB writable layers. Images total 6.804 GB with 2.59 GB reclaimable; 13 local volumes total 2.053 GB with 1.944 GB unused but including persistent databases from other projects; build cache is 138.5 MB with only 529.3 KB reclaimable. No prune or cleanup ran.
- The inactive image set is ordinary version/mirror sediment rather than repeated full copies: an older Postgres image contributes about 1.295 GB unique, an older storage-api about 366.7 MB, plus unused auth, edge-runtime, pg_prove, and two recent worker build results. Docker content currently accounts for material baseline space but not the live rapid drain.
- A 20-second live check with all nine containers running grew `Docker.raw` only 32 KiB. Over the following roughly three minutes, host free space fell from about 6.6 GiB to 3.35 GiB while `Docker.raw` grew only about 216 KiB. The matched writer is an unowned concurrent Codex release-gate process running `xcodebuild test` with simulator outputs under its temporary release-gate tree; it began after the daemon recovered and remains active. It was not interrupted because it belongs to another work session. Docker was left running for that test.

## 2026-08-30T10:14:39+0800 Docker cleanup and Article Nudge Xcode-environment research

- After the user explicitly expanded scope to Docker cleanup and the Article Nudge product repository, removed only rebuildable Docker content: one dangling image reference, 144.73 MB across two build-cache passes, and all seven tagged images not referenced by a container. The tagged-image prune reported 2.452 GB reclaimed. Docker now has 9 active / 0 unused images, 9 running / 0 stopped containers, and 0B build cache; daemon `_ping` is `OK`.
- Preserved all 13 named volumes. Docker reports 2.053 GB total volume data and 1.944 GB unused, but those unused volumes include persistent PostgreSQL, Open WebUI, and Redis data from other projects and were not treated as cache. `Docker.raw` physical allocation fell from 14,812,896 KiB before cleanup to 11,656,472 KiB after prune/TRIM; no reset or volume deletion ran.
- Deep Article Nudge investigation found that the delivery toolchain is aligned but the complete development environment is not hermetic. The host, foundation gate, and current GitHub runner evidence agree on Xcode 26.6 build 17F113, Swift 6.3.3, iOS SDK/Simulator 26.5, exact project Package.resolved pins, and a disposable exact-runtime simulator. Primary and temporary release-gate commits had identical blobs for the toolchain script, workflow, project, and both lockfiles. The delivery-toolchain loop is GREEN.
- Day-to-day preview and personal-device entry points do not run the exact Xcode/Swift/SDK preflight, the foundation gate does not set `-derivedDataPath` or a scoped module cache, and the repository has no cache retention/cleanup contract. Measured retained pools were primary `.build` 10.47 GiB, temporary gate `.build` 2.69 GiB, total host Xcode DerivedData 12.76 GiB including 2.47 GiB Article Nudge data and 4.65 GiB ModuleCache, plus 8.60 GiB CoreSimulator device data. The all-entrypoint and cache-boundary loops are RED.
- Added the first-party-source research note `docs/xcode-environment-reproducibility-2026-08-30.md` in the Article Nudge repository and validated it with `git diff --check`. No product source, existing dirty `MEMORY.md`, branch, commit, remote, issue, or product data was changed. The unowned concurrent Codex session was not interrupted.

## 2026-08-31T10:16:13+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or product work was read or changed, and no MCP server started.
- Refreshed Homebrew metadata/runtime `b42173be7f -> db1f209a10` (`6.0.20-54 -> 6.0.20-66`) and upgraded Mole `1.52.0 -> 1.53.0`. Formula linkage, direct runtime, `brew missing`, and Homebrew Doctor passed. Archon stays `0.9.0` while its untrusted third-party tap offers `0.10.1`; retry only with exact source-trust approval.
- Refreshed all eight Claude Git marketplaces and checked all seven user-scope marketplace plugins with official commands; versions, scopes, and enabled states are unchanged. Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; reconcile/runtime and real-directory/source validation passed for 24 managed skills, with accepted `code-review` and `grilling` local deltas preserved.
- Claude Code native `2.1.251` and Codex standalone `0.151.0` match official latest metadata. Claude Doctor reports a healthy current install despite a recorded `install_failed` auto-update attempt; retry only on a version gap or repeated failure. Codex's configured Git marketplace is current, Doctor reports 0 failures and healthy databases, and 2,980 active rollout files remain missing state rows; retry repair only when a supported interface exists.
- Config-only MCP auditing found fixed Playwright `0.0.79` and GCP `0.5.3` plus floating Chrome DevTools MCP `1.8.0` current. Fixed Context7 `4.0.3` remains held while registry latest is `4.0.4`; retry only with reviewed compatibility or migration evidence. Notion/Slack and remote/App-managed auth remain skipped, and no MCP list/health/server command ran.
- `code-review-graph` and Voice Mode stayed `2.3.8` and `8.12.0` while their isolated `authlib 1.7.2 -> 1.8.0` updated through permitted `uv tool upgrade` paths; Pyright stayed `1.1.411`. CRG version validation passed, Graphify remains absent, and no installer, graph, schema, server, daemon, embed, or hook command ran.
- Node/default `24.20.0`, npm `12.0.2`, Corepack `0.36.0`, pnpm `11.24.0`, Bun `1.4.0`, gh `2.98.0`, uv `0.12.7`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current; global package-name sets are unchanged. Mise stays brew-managed at `2026.8.14` while upstream reports `2026.8.15`; retry when Homebrew offers it. Vercel remains policy-selected at `59.3.0` because pnpm `wanted` is unchanged.
- Deferred GUI casks are Block Buzz `0.5.18 -> 0.5.20`, CodexBar `0.56.0 -> 0.56.1`, and McBopomofo `3.0 -> 3.1`. Credential checks confirmed Claude `claude.ai`, Codex ChatGPT file storage, and gh keyring sources without exposing values.

## 2026-08-31T10:33:50+0800 approved manual-maintenance follow-up

- User approved every manual item from the daily checkpoint except Notion/Slack authorization. Completed the projectless follow-up from `$HOME` without entering a product repo or Orca workspace and without changing schedulers, project state, graph data, credentials, or OAuth state.
- With exact source-trust approval, upgraded Archon `0.9.0 -> 0.10.1`. Upgraded Block Buzz `0.5.18 -> 0.5.20` and CodexBar `0.56.0 -> 0.56.1` through Homebrew. Formula linkage/runtime, cask receipts/bundle versions, strict code signatures, Gatekeeper, `brew missing`, and Homebrew Doctor passed; no formula remains outdated.
- Reviewed the published Context7 `4.0.3..4.0.4` package delta: Node requirement, dependencies, binary, command, and config shape are unchanged; the release is a client-IP assertion/proxy-trust security update. Updated the Claude enabled pin and both disabled Codex user-config pins `4.0.3 -> 4.0.4`; JSON/TOML parse, mirror equivalence, registry resolution, Claude Doctor, and Codex Doctor passed without starting an MCP server.
- Updated the actual user-level McBopomofo bundle `3.0 -> 3.1` with the official SHA-verified, signed, notarized OpenVanilla installer. The installed bundle is `3.1` build `2496`, passes strict code-signature/Gatekeeper checks, and its input-method process was restored after the installer stopped the old process. Homebrew still records receipt `3.0` because its manual-installer reinstall path requires interactive sudo; the failed receipt-only alignment attempt left the healthy `3.1` app intact. Retry key `mcbopomofo-homebrew-receipt` only with an interactive sudo-capable maintenance session.
- Notion/Slack OAuth remains explicitly skipped. The former `archon-source-trust`, `context7-fixed-pin`, Block Buzz, CodexBar, and McBopomofo bundle-version manual items are closed.

## 2026-09-01T09:12:42+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or product work was entered or changed, and no MCP server started.
- Claude Code advanced through its healthy native auto-updater `2.1.251 -> 2.1.252`; Doctor confirms latest-channel health and a successful last update. Refreshed all eight configured Git marketplaces and all seven user-scope marketplace plugins while preserving scope/enabled state; all HEADs and plugin versions remain current. Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; reconcile/runtime, source, real-directory, symlink, `SKILL.md`, and managed-subset validation passed for 24 skills, with the accepted `code-review` and `grilling` local policy deltas preserved.
- Refreshed Homebrew metadata/runtime `6bf9a4710622 -> 518895ab7be7` (`6.0.20-69 -> 6.0.20-73`) and upgraded expat `2.8.3 -> 2.8.4`, gpgme `2.1.2 -> 2.2.0`, mise `2026.8.14 -> 2026.8.16`, OpenSSL 3 `3.6.3 -> 3.6.4`, and usage `6.5.0 -> 6.6.0` from the exact dry run. All five linkage/runtime checks, `brew missing`, and Homebrew Doctor passed; no formula remains outdated and every updated formula came from `homebrew/core`.
- Updated Corepack-managed pnpm `11.24.0 -> 11.25.0` and standalone uv `0.12.7 -> 0.12.8`. `code-review-graph` stayed `2.3.8` while isolated cachetools updated `7.1.7 -> 7.1.8`; Voice Mode stayed `8.12.0` while isolated cachetools and uv updated, and Pyright stayed `1.1.411`. CRG used only its permitted upgrade/version path; Graphify remains absent and no installer, graph, schema, server, daemon, embed, hook, or repo command ran.
- Config-only MCP auditing confirmed fixed Context7 `4.0.4`, Playwright `0.0.79`, and GCP `0.5.3` plus floating Chrome DevTools MCP `1.8.0` are current. Remote/App-managed auth and Notion/Slack OAuth remain skipped; user JSON/TOML parse passed and no MCP list/health/server command ran.
- Codex standalone `0.151.0` matches official registry and cached latest metadata; its Git marketplace remains current. Doctor reports 0 failures and healthy databases, with `3,016` of `3,897` active rollout files missing state rows plus the existing ignored local-config warning. Retry `codex-rollout-repair` only when Codex exposes a supported repair interface; no database or rollout mutation ran.
- Node/default `24.20.0`, npm `12.0.2`, Corepack `0.36.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.53.0`, rustup `1.29.0`, and Rust/rustc/cargo `1.98.0` remain current. npm Doctor passed after repairing only its rebuildable cache index; npm/pnpm/Bun global package-name sets and Claude/Codex/gh credential source types are unchanged.
- Evidence-gated retry keys: Vercel stays policy-selected at `59.3.0` because pnpm `wanted` is unchanged while registry latest is `59.10.0`; GUI casks remain CodexBar `0.56.1 -> 0.56.2`, McBopomofo receipt `3.0 -> 3.1` while the installed bundle is already `3.1` build `2496`, and Orca receipt `1.4.168 -> 1.4.193`. Retry only on changed policy evidence, explicit GUI approval, an interactive sudo-capable receipt-alignment session, or a supported ownership repair.

## 2026-09-01T22:27:05+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or product work was entered or changed, and no MCP server started.
- Updated the Orca-owned Codex standalone `0.151.0 -> 0.152.0` with the official checksum-verifying installer and existing owner `CODEX_HOME`. Its Git marketplace is current; Doctor reports 0 failures, healthy databases, and 417 stale rollout rows with no missing active rows. Retry `codex-rollout-repair` only when Codex exposes a supported repair interface.
- Refreshed Homebrew `6.0.20-77-g624cf71 -> 6.0.21-8-g28236b8` and upgraded `gnupg 2.5.21 -> 2.5.22`, `gpgmepp 2.1.0 -> 2.2.0`, `mise 2026.8.16 -> 2026.9.0`, and `pcre2 10.47_1 -> 10.48` from the exact dry run. All four linkage/runtime checks, `brew missing`, and Homebrew Doctor passed; no formula remains outdated and all four targets came from `homebrew/core`.
- Updated rustup `1.29.0 -> 1.29.1`. `code-review-graph` and Voice Mode stayed `2.3.8` and `8.12.0` while isolated `platformdirs 4.11.5 -> 4.11.7` refreshed; Pyright stayed `1.1.411`. CRG used only its permitted upgrade/version path, Graphify remains absent, and no prohibited graph or repo operation ran.
- Claude Code remains current at native `2.1.252`; Doctor reports no installation issue despite one recorded `install_failed` auto-update attempt. All eight Git marketplaces and seven user-scope marketplace plugins are current with scope/enabled state preserved. Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; reconcile/runtime and source/plugin validation passed for 24 skills, with the accepted `grilling` local policy delta preserved.
- Config-only MCP auditing found fixed Context7 `4.0.4` and GCP `0.5.3` plus floating Chrome DevTools MCP `1.8.0` current. Fixed Playwright `0.0.79` is held while registry latest is `0.0.80`; retry only with reviewed compatibility or migration evidence. Notion/Slack and remote/App-managed auth remain skipped; no MCP list/health/server command ran.
- Node/default `24.20.0`, npm `12.0.2`, Corepack `0.36.0`, pnpm `11.25.0`, Bun `1.4.0`, gh `2.98.0`, Mole `1.53.0`, uv `0.12.8`, and Rust/rustc/cargo `1.98.0` remain current; runtime paths, credential source types, and npm/pnpm/Bun global package-name sets are unchanged. Vercel remains policy-selected at `59.3.0` because pnpm `wanted` is unchanged. Evidence-gated GUI keys remain CodexBar `0.56.1 -> 0.56.2`, McBopomofo receipt alignment, and Orca ownership repair; Notion/Slack OAuth remains explicitly skipped.

## 2026-09-02T09:51:05+0800 daily development-tool update

- Completed the unattended projectless update from `$HOME`; no product repo, Orca workspace, project dependency, lockfile, `.mcp.json`, graph/hook, OAuth state, secret, scheduler configuration, or product work was entered or changed, and no MCP server started.
- Updated Claude Code native `2.1.252 -> 2.1.258`; Doctor confirms the current install and no installation issue, although its recorded last auto-update attempt remains the older `install_failed` event. Refreshed all eight Git marketplaces and seven user-scope marketplace plugins with scope/enabled state preserved; disabled `code-review` updated `4b909c3492b3 -> e18ff5086423` and the other six remain current.
- Matt took the fast path at unchanged HEAD `6654f6b60cd9` and manifest `1.2.3`; marketplace/source validation passed and the 24-skill managed subset is current. Claude `2.1.258` now rejects the self-hosted `skills` symlink because it resolves outside the plugin directory, so runtime reconciliation reports all 24 skills missing. Preserved the existing `improve-codebase-architecture`, `grilling`, and `writing-for-agents` local deltas; retry `matt-plugin-path-escape` only with a supported runtime/layout change, and retry local-delta reconciliation only when upstream changes.
- Updated the Orca-owned Codex standalone `0.152.0 -> 0.152.1` with the official checksum-verifying installer after the built-in updater could not detect its owner. Git marketplaces are current; Doctor reports 0 failures and healthy databases, with the existing 417 stale rollout rows and no missing active rows. Retry `codex-rollout-repair` only when Codex exposes a supported repair interface.
- Refreshed Homebrew `66e8d3d36dc2 -> d28e78518da4`, then upgraded trusted core formulae gh `2.98.0 -> 2.99.0` and usage `6.6.0 -> 6.6.1`. Updated both user-global owners of Shopify CLI `4.7.0 -> 4.7.1`, standalone uv `0.12.8 -> 0.12.9`, and Voice Mode isolated dependencies `openai 3.6.0 -> 3.7.0` plus `uv 0.12.8 -> 0.12.9`; all runtime, linkage, `brew missing`, and Homebrew Doctor gates passed.
- Config-only MCP auditing found fixed Context7 `4.0.4` and GCP `0.5.3` plus floating Chrome DevTools MCP `1.8.0` current. Fixed Playwright `0.0.79` remains held while registry latest is `0.0.80`; Notion/Slack and remote/App-managed auth remain skipped. CRG `2.3.8` and Pyright `1.1.411` are current, Graphify remains absent, and no MCP list/health/server or prohibited graph command ran.
- Node/default `24.20.0`, npm `12.0.2`, Corepack `0.36.0`, pnpm `11.25.0`, Bun `1.4.0`, Mole `1.53.0`, mise `2026.9.0`, rustup `1.29.1`, and Rust/rustc/cargo `1.98.0` match official metadata; global package-name sets and credential source types are unchanged. `rustup --version` printed `1.29.1` but did not exit and was terminated; direct stable `rustc`/`cargo` checks passed, so only the rustup health command is unresolved.
- Evidence-gated holds: untrusted-tap OpenCode `1.18.25 -> 1.18.26` requires exact source-trust approval; Vercel remains policy-selected at `59.3.0` because pnpm `wanted` is unchanged; GUI casks CodexBar `0.56.1 -> 0.56.2` and McBopomofo receipt alignment require explicit GUI/sudo handling. Notion/Slack OAuth remains explicitly skipped.

## 2026-09-02T10:19:47+0800 Claude Matt runtime containment follow-up

- Replaced the rejected cross-plugin `skills` symlink with a generated, self-contained runtime sourced only from `~/.claude/plugins/marketplaces/mattpocock`. The exact 24 upstream-selected skills load under Claude `2.1.258`; upstream `handoff` remains excluded, and the separate `~/.agents` local deltas remain untouched.
- `~/.claude/scripts/reconcile_matt_manifest.py` is now the sole writer for both the local manifest and derived runtime. It validates source containment, rejects symlinks, stages a complete copy before replacement, checks exact file content and executable modes, preserves the previous runtime on replacement failure, and rejects Claude loading errors or inventory drift.
- Verification passed: all 15 user-level tests, reconcile `--check --runtime`, strict plugin validation, a 24-skill Claude inventory with no loading errors, no runtime symlinks or staging/backup residue, and Claude Doctor with no installation issue.
- Closed retry key `matt-plugin-path-escape`; reopen only if the upstream manifest/layout or Claude containment behavior changes. No product repo or Orca workspace was entered, and no scheduler, plugin cache, OAuth state, credential, MCP server, graph, or project state was modified.
